---
phase: 02-ux-polish-testing-interactions-animations
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/data/personal-projects.ts
  - public/screenshots/.gitkeep
autonomous: false
requirements:
  - UX-10
  - UX-11

user_setup:
  - service: screenshots
    why: "Luiz must provide real PNG screenshots (1280×800 min) for 4 personal projects before the data file can be updated"
    dashboard_config:
      - task: "Export or capture screenshots of DoAção, NotificaMe, MTG Price Monitor, Redzone Boss at ≥1280px wide"
        location: "Browser dev tools device simulation or Screenshotone/Playwright export — Luiz's choice"
      - task: "Rename and place files in public/screenshots/ per naming convention below"
        location: "public/screenshots/doacao.png, public/screenshots/notificame.png, public/screenshots/mtgprice.png, public/screenshots/redzone-boss.png"

must_haves:
  truths:
    - "The 4 personal projects (DoAção, NotificaMe, MTG Price Monitor, Redzone Boss) render real screenshots instead of gradient placeholders"
    - "The screenshots load from /screenshots/[id].png paths via next/image"
    - "Starlimp card is unchanged (continues to load /personal-projects/starlimp.png)"
    - "The scroll-reveal stagger (RevealGroup stagger={0.07}) on the PersonalProjects grid still fires as cards enter the viewport"
    - "public/screenshots/ directory exists before personal-projects.ts is updated"
  artifacts:
    - path: src/data/personal-projects.ts
      provides: "screenshot field populated for doacao, notificame, mtgprice, redzone-boss"
      contains: "screenshot: '/screenshots/doacao.png'"
    - path: public/screenshots/
      provides: "Directory containing 4 project PNG screenshots"
  key_links:
    - from: personal-projects.ts screenshot fields
      to: public/screenshots/ directory
      via: "next/image src prop in ProjectScreenshot component — path /screenshots/[id].png resolves to public/screenshots/[id].png"
      pattern: "screenshot: '/screenshots/"
---

<objective>
Wire real project screenshots into the Personal Projects section and verify the scroll-reveal stagger animations remain intact after the change.

Purpose: Addresses UX-10 (Personal Projects visual quality — real screenshots instead of gradient placeholders) and confirms UX-11 (stagger animations already implemented, verified post-change). Screenshots require a human hand-off checkpoint because Luiz must provide the PNG assets.
Output: public/screenshots/ directory with 4 PNGs; personal-projects.ts with screenshot fields populated for 4 projects; visual verification that RevealGroup stagger still fires.
</objective>

<execution_context>
@/Users/luizpansarini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luizpansarini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ux-polish-testing-animations/02-CONTEXT.md
@.planning/phases/02-ux-polish-testing-animations/02-01-SUMMARY.md
@.planning/phases/02-ux-polish-testing-animations/02-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/data/personal-projects.ts (full current state):
- PersonalProject interface: { id, name, description, stack, status, screenshot?: string, liveUrl?, githubUrl?, accentColor }
- screenshot field comment: "relative path under /personal-projects/" — NOTE: new screenshots go in /screenshots/ not /personal-projects/
- Projects needing screenshot: id='doacao', id='notificame', id='mtgprice', id='redzone-boss' (all currently have no screenshot field)
- Already has screenshot: id='starlimp' with screenshot: '/personal-projects/starlimp.png' — DO NOT TOUCH

From src/components/sections/personal-projects.tsx (ProjectScreenshot component, lines 37–59):
- Branch: if (project.screenshot) → renders next/image with src={project.screenshot}
- image props: width=640, height=400, sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 400px"
- className: "h-full w-full object-cover object-top transition-transform duration-500 group-hover:scale-105"
- Else: renders gradient div with accentColor — keep accentColor on all 4 entries even after adding screenshot (serves as fallback)

From src/components/sections/personal-projects.tsx (RevealGroup, line 81):
- RevealGroup stagger={0.07} wraps all 5 cards — this is UX-11 already implemented; do not modify

Screenshot naming convention (from CONTEXT.md D-06 + UI-SPEC):
- public/screenshots/doacao.png → screenshot: '/screenshots/doacao.png'
- public/screenshots/notificame.png → screenshot: '/screenshots/notificame.png'
- public/screenshots/mtgprice.png → screenshot: '/screenshots/mtgprice.png'
- public/screenshots/redzone-boss.png → screenshot: '/screenshots/redzone-boss.png'
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Luiz provides 4 project screenshots</name>
  <what-built>Directory public/screenshots/ must be created and populated before the data file can be updated. This directory does not yet exist. The .gitkeep placeholder created by Task 0 marks the directory for git tracking.</what-built>
  <how-to-verify>
    1. Export or capture screenshots of these 4 projects at ≥1280px wide, PNG format:
       - DoAção (github.com/LuizHAP/doacao) → save as public/screenshots/doacao.png
       - NotificaMe (github.com/LuizHAP/notificame) → save as public/screenshots/notificame.png
       - MTG Price Monitor (github.com/LuizHAP/mtgprice) → save as public/screenshots/mtgprice.png
       - Redzone Boss (github.com/endzone-manager/website) → save as public/screenshots/redzone-boss.png

    2. Keep images ≤1280px wide to stay within Vercel Hobby image optimization quota (5K/month).
       next/image will serve optimized WebP at the sizes declared in ProjectScreenshot.

    3. Verify all 4 files exist:
       ls public/screenshots/

    Expected output: doacao.png  mtgprice.png  notificame.png  redzone-boss.png  .gitkeep
  </how-to-verify>
  <resume-signal>Type "screenshots ready" once all 4 PNG files are in public/screenshots/</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Create public/screenshots/ directory and update personal-projects.ts</name>
  <files>
    public/screenshots/.gitkeep
    src/data/personal-projects.ts
  </files>
  <read_first>
    src/data/personal-projects.ts
    src/components/sections/personal-projects.tsx
  </read_first>
  <action>
    Step A — Create directory placeholder:
    Create public/screenshots/.gitkeep as an empty file so the directory is tracked by git even before Luiz's PNGs are committed. This must happen before the data file update so next/image path resolution is available.

    Step B — Update personal-projects.ts:
    Read the file first. For each of the 4 projects (id: 'doacao', 'notificame', 'mtgprice', 'redzone-boss'), add a screenshot field immediately before the accentColor field. Exact values:
    - doacao: `screenshot: '/screenshots/doacao.png'`
    - notificame: `screenshot: '/screenshots/notificame.png'`
    - mtgprice: `screenshot: '/screenshots/mtgprice.png'`
    - redzone-boss: `screenshot: '/screenshots/redzone-boss.png'`

    Keep all other fields unchanged including accentColor (it is the fallback for ProjectScreenshot if next/image fails). The starlimp entry is untouched. The PersonalProject interface already declares screenshot as optional string — no interface changes needed.

    The path prefix is /screenshots/ (not /personal-projects/) matching the new directory. This is different from Starlimp's /personal-projects/ prefix.
  </action>
  <verify>
    <automated>grep -n "screenshot" /Users/luizpansarini/Documents/Projetos/Pessoal/pansarinitech/src/data/personal-projects.ts</automated>
  </verify>
  <done>
    grep output shows 5 screenshot entries total: /personal-projects/starlimp.png (unchanged) + /screenshots/doacao.png + /screenshots/notificame.png + /screenshots/mtgprice.png + /screenshots/redzone-boss.png. public/screenshots/.gitkeep exists.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify screenshots render + stagger animations fire</name>
  <what-built>personal-projects.ts now has screenshot fields for all 4 previously-gradient projects. The ProjectScreenshot component will render next/image for all 5 cards. RevealGroup stagger={0.07} is unchanged.</what-built>
  <how-to-verify>
    1. Start the dev server: pnpm dev
    2. Open http://localhost:3000/en (or /pt) and scroll to the Personal Projects section.
    3. Verify each of the 4 updated cards shows its real screenshot (not a gradient placeholder):
       - DoAção card: shows doacao.png
       - NotificaMe card: shows notificame.png
       - MTG Price Monitor card: shows mtgprice.png
       - Redzone Boss card: shows redzone-boss.png
    4. Starlimp card continues to show the existing starlimp.png screenshot — no change.
    5. Refresh the page and scroll down slowly: confirm all 5 cards animate in with a staggered fade-up entry (each card enters ~70ms after the previous). This confirms UX-11 is intact.
    6. Hover over any card: confirm the screenshot image gently scales (group-hover:scale-105 effect).
    7. Open browser DevTools Network tab, filter by Img — confirm next/image serves the screenshots (status 200, WebP format from Vercel image optimization or dev mode).
  </how-to-verify>
  <resume-signal>Type "visual approved" or describe any issues (broken images, missing stagger, layout problems)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static asset path | personal-projects.ts screenshot paths are hardcoded strings resolved at build time by next/image; no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | public/screenshots/ | accept | Screenshots show public project UIs — no PII or credentials; assets are intentionally public |
| T-02-05 | Denial of Service | next/image optimization quota | accept | Assets pre-sized to ≤1280px reduces optimization requests; Vercel Hobby 5K/month quota noted in user_setup instructions |
</threat_model>

<verification>
After the visual checkpoint passes, run the full test suite to confirm no regressions from the data file update:

```
cd /Users/luizpansarini/Documents/Projetos/Pessoal/pansarinitech && pnpm test --run 2>&1 | tail -10
```

Existing personal-projects tests use `toBeGreaterThanOrEqual(1)` for name text assertions — these are screenshot-change-safe per RESEARCH.md Pitfall 3 analysis.

Confirm the 5 screenshot entries in data file:

```
grep -c "screenshot:" /Users/luizpansarini/Documents/Projetos/Pessoal/pansarinitech/src/data/personal-projects.ts
```

Expected: 5
</verification>

<success_criteria>
- public/screenshots/.gitkeep exists (directory tracked by git)
- personal-projects.ts has screenshot field on all 5 entries (5 total; 1 pre-existing Starlimp + 4 new)
- All 4 new screenshot paths use /screenshots/ prefix (not /personal-projects/)
- accentColor kept on all 4 updated entries as fallback
- Visual checkpoint: 4 cards show real screenshots; Starlimp unchanged; stagger animation fires on scroll; image zoom on hover works
- Full pnpm test run exits 0 with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-ux-polish-testing-interactions-animations/02-03-SUMMARY.md`
</output>
