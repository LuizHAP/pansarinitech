---
phase: 07-blog-post-seo-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/blog/view-transitions-theme-toggle.en.mdx
  - content/blog/view-transitions-theme-toggle.pt.mdx
autonomous: true
requirements:
  - BLOG-01
  - BLOG-02

must_haves:
  truths:
    - "The post appears in the blog listing at /en/blog and /pt/blog with correct title, date, and tags"
    - "The post body uses at least one <Callout>, one <CodeFilename>, and one <InlineBadge> — demonstrating the Phase 6 toolkit"
    - "Both MDX files pass the PostFrontmatter Zod schema validation on build (pnpm build exits 0)"
    - "The post renders without 404 on /en/blog/view-transitions-theme-toggle and /pt/blog/view-transitions-theme-toggle"
    - "Code snippets quoted in the post match the actual source in theme-toggle.tsx and globals.css"
  artifacts:
    - path: "content/blog/view-transitions-theme-toggle.en.mdx"
      provides: "EN blog post — View Transitions API on the Theme Toggle"
      contains: "draft: false"
    - path: "content/blog/view-transitions-theme-toggle.pt.mdx"
      provides: "PT blog post — View Transitions API no Alternador de Tema"
      contains: "draft: false"
  key_links:
    - from: "content/blog/view-transitions-theme-toggle.en.mdx"
      to: "src/app/[locale]/blog/[slug]/page.tsx"
      via: "dynamic [slug] route reads slug from filename"
      pattern: "view-transitions-theme-toggle"
    - from: "content/blog/view-transitions-theme-toggle.en.mdx"
      to: "src/components/mdx/index.ts"
      via: "mdxComponents map provides Callout, CodeFilename, InlineBadge globally"
      pattern: "mdxComponents"
---

<objective>
Author and publish the "View Transitions API on the Theme Toggle" blog post in both EN and PT locales. Both files are code-heavy (~1500 words each), use the Phase 6 MDX toolkit (Callout, CodeFilename, InlineBadge), and follow the established frontmatter schema and content pipeline constraints.

Purpose: Fulfil BLOG-01 and BLOG-02 — put a real, technically-grounded post live on both locales that showcases the Phase 6 component toolkit in authentic prose.
Output: Two MDX files at content/blog/view-transitions-theme-toggle.{en,pt}.mdx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-blog-post-seo-hardening/07-CONTEXT.md
@.planning/phases/07-blog-post-seo-hardening/07-PATTERNS.md
@.planning/phases/07-blog-post-seo-hardening/07-UI-SPEC.md
</context>

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From content/blog/building-this-portfolio.en.mdx (frontmatter schema):
```yaml
---
title: "Building this portfolio"
date: "2026-05-02"
excerpt: "Architectural diary of a bilingual, mobile-first, AA-accessible portfolio. Stack rationale, contrast fixes, MDX factory pattern, and what got cut."
tags:
  - nextjs
  - tailwind
  - i18n
  - mdx
  - biome
  - accessibility
draft: false
---
```

MDX component usage (from scripts/generate-post-prompt.md):
- Callout: `<Callout type="info">prose content</Callout>` — no import needed
- CodeFilename: `<CodeFilename filename="src/components/shared/theme-toggle.tsx">` then fenced code block then `</CodeFilename>` — no import needed
- InlineBadge: inline in prose as `<InlineBadge variant="secondary">Chrome 111+</InlineBadge>` — no import needed

Content constraints (blockJS: true defaults in next-mdx-remote@6):
- NO {expr} expressions, NO import statements, NO export statements inside MDX body
- H2 headings only in body (page template renders frontmatter title as H1)
- Code blocks: fenced triple-backtick only

Real code from theme-toggle.tsx (lines 28-61) — the onClick handler:
- Feature-detect: checks typeof document, 'startViewTransition' in document, and prefers-reduced-motion
- Coord capture: rect = event.currentTarget.getBoundingClientRect(); x = event.clientX || center; y = event.clientY || center
- CSS custom properties: document.documentElement.style.setProperty('--vt-x', `${x}px`) and '--vt-y'
- Transition call: document.startViewTransition(() => { setTheme(next); })

Real CSS from globals.css (lines 146-198):
- vt-reveal keyframe: clip-path from circle(0% at var(--vt-x, 50%) var(--vt-y, 50%)) to circle(150% ...)
- Animation on ::view-transition-new(root): vt-reveal 420ms cubic-bezier(0.4, 0, 0.2, 1) forwards
- Reduced-motion fallback: vt-fade 200ms linear forwards !important on both old and new
- vt-fade keyframe: opacity 1 → 0; new(root) runs in reverse for cross-fade effect
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author EN blog post — view-transitions-theme-toggle.en.mdx</name>
  <files>content/blog/view-transitions-theme-toggle.en.mdx</files>
  <read_first>
    - src/components/shared/theme-toggle.tsx (full file — onClick handler is the core code to quote)
    - src/app/globals.css lines 130-200 (VT keyframes and reduced-motion override blocks)
    - content/blog/building-this-portfolio.en.mdx (frontmatter schema reference)
    - scripts/generate-post-prompt.md (MDX component usage rules and blockJS constraints)
    - src/components/mdx/index.ts (confirm CodeFilename and InlineBadge are registered)
  </read_first>
  <action>
Create content/blog/view-transitions-theme-toggle.en.mdx.

Frontmatter (per D-03, D-04, Claude's Discretion):
- title: "View Transitions API on the Theme Toggle"
- date: "2026-05-19"
- excerpt: 40–280 characters, technical summary of the View Transitions approach
- tags: [nextjs, view-transitions, css, animations, accessibility]
- draft: false

Body structure (per D-06, ~1500 words):
1. H2 "Browser support" — View Transitions API status across Chromium (111+), Safari (18+), Firefox (144+); mention progressive enhancement (instant fallback on unsupported browsers)
2. H2 "How the radial reveal works" — explain the mental model: startViewTransition takes OLD snapshot, callback runs setTheme, then CSS animates from OLD to NEW; the clip-path grows from click coordinates
3. H2 "Setting up the clip-path animation" — walk through the CSS: @supports wrapper, ::view-transition-old(root) animation: none; ::view-transition-new(root) vt-reveal 420ms; the vt-reveal @keyframes from circle(0% at --vt-x --vt-y) to circle(150%). Quote real CSS from globals.css inside a CodeFilename block
4. H2 "Reduced-motion fallback" — place the Callout here (type="info", content about why this matters for vestibular users). Explain the two-layer approach: JS bypasses startViewTransition entirely when matchMedia('(prefers-reduced-motion: reduce)').matches; CSS also applies vt-fade 200ms cross-fade for engines that still run the snapshot. Quote the prefers-reduced-motion block from globals.css
5. H2 "Jedi and Sith palette swap" — explain that setTheme writes the .dark class on <html>; the OKLCH color variables swap; the clip-path reveals the new palette from the click origin — creating the "lightsaber igniting from the toggle" effect
6. H2 "The toggle handler" — full code walkthrough of onClick from theme-toggle.tsx, placed inside a CodeFilename block with filename="src/components/shared/theme-toggle.tsx". Use InlineBadge for browser support callouts inline in this section (e.g., `<InlineBadge variant="secondary">Chrome 111+</InlineBadge>`)
7. H2 "What this unlocks" — brief closing: progressive enhancement means recruiters on older browsers get the instant swap; users who prefer reduced motion get the controlled fade; everyone else gets the radial reveal

MDX toolkit requirements (per D-05, BLOG-01 SC-2):
- At least one `<Callout type="info">` — place before or within the reduced-motion section
- At least one `<CodeFilename filename="src/components/shared/theme-toggle.tsx">` — wrap the main onClick code snippet
- At least one `<InlineBadge variant="secondary">` — inline in prose for browser support, e.g. Chrome 111+

Do NOT add any import statements, export statements, or {expr} JS expressions in the MDX body (blockJS: true strips them silently).
Code snippets must be fenced triple-backtick blocks.
Use first-person past tense ("I chose", "I bypassed").
Voice: technical, direct, principal-engineer level.
  </action>
  <verify>
    <automated>cd /Users/luizpansarini/Documents/Projetos/Pessoal/pansarinitech && grep -c "draft: false" content/blog/view-transitions-theme-toggle.en.mdx && grep -c "CodeFilename" content/blog/view-transitions-theme-toggle.en.mdx && grep -c "InlineBadge" content/blog/view-transitions-theme-toggle.en.mdx && grep -c "Callout" content/blog/view-transitions-theme-toggle.en.mdx</automated>
  </verify>
  <acceptance_criteria>
    - content/blog/view-transitions-theme-toggle.en.mdx exists with valid YAML frontmatter block (title, date, excerpt, tags array, draft: false)
    - File contains at least one `<Callout` usage
    - File contains at least one `<CodeFilename filename="src/components/shared/theme-toggle.tsx">` usage
    - File contains at least one `<InlineBadge variant="secondary">` usage
    - File contains no `import ` or `export ` statements in the body (outside frontmatter)
    - File contains no `{` expressions in body prose (code blocks excepted)
    - Body headings are H2 only (no H1 in body — title rendered by page template)
    - Word count approximately 1400–1600 words in the body
  </acceptance_criteria>
  <done>EN blog post file exists, passes frontmatter schema, uses all three toolkit components, and contains code-accurate prose sourced from theme-toggle.tsx and globals.css</done>
</task>

<task type="auto">
  <name>Task 2: Author PT blog post — view-transitions-theme-toggle.pt.mdx</name>
  <files>content/blog/view-transitions-theme-toggle.pt.mdx</files>
  <read_first>
    - content/blog/view-transitions-theme-toggle.en.mdx (just authored — same structure, different prose)
    - content/blog/building-this-portfolio.pt.mdx (PT frontmatter reference — tone, idiomatic PT-BR)
    - scripts/generate-post-prompt.md (MDX component usage rules)
  </read_first>
  <action>
Create content/blog/view-transitions-theme-toggle.pt.mdx.

Frontmatter (per D-03, same date/tags as EN):
- title: "View Transitions API no Alternador de Tema"
- date: "2026-05-19"
- excerpt: idiomatic PT-BR summary, 40–280 characters — NOT a word-for-word translation
- tags: [nextjs, view-transitions, css, animations, accessibility] (same slugs as EN)
- draft: false

Body: Same six-section structure as the EN post. Reuse the same code snippets verbatim (code is language-neutral). Rethink the prose narrative in each section in idiomatic Brazilian Portuguese — do NOT translate the EN text word-for-word. First-person past tense in PT ("escolhi", "decidi", "implementei"). Technical vocabulary in PT-BR conventions: "animação", "transição de visualização", "acessibilidade", "paleta de cores", "alternador de tema".

MDX toolkit placement: identical structural positions as EN post — same Callout, CodeFilename, InlineBadge occurrences in the same sections.

Do NOT add import, export, or {expr} in the MDX body.
  </action>
  <verify>
    <automated>cd /Users/luizpansarini/Documents/Projetos/Pessoal/pansarinitech && grep -c "draft: false" content/blog/view-transitions-theme-toggle.pt.mdx && grep -c "CodeFilename" content/blog/view-transitions-theme-toggle.pt.mdx && grep -c "InlineBadge" content/blog/view-transitions-theme-toggle.pt.mdx && grep -c "Callout" content/blog/view-transitions-theme-toggle.pt.mdx</automated>
  </verify>
  <acceptance_criteria>
    - content/blog/view-transitions-theme-toggle.pt.mdx exists with valid YAML frontmatter (title in Portuguese, date "2026-05-19", excerpt in PT-BR, same tags array, draft: false)
    - File contains at least one `<Callout` usage in same structural position as EN
    - File contains at least one `<CodeFilename filename="src/components/shared/theme-toggle.tsx">` usage
    - File contains at least one `<InlineBadge` usage
    - File contains no `import ` or `export ` statements in the body
    - File contains no `{` expressions in body prose
    - Body headings are H2 only
    - Prose is idiomatic PT-BR, not a literal translation (different sentence constructions, PT-BR technical terms)
  </acceptance_criteria>
  <done>PT blog post file exists, passes frontmatter schema, uses all three toolkit components, prose is idiomatic PT-BR, code snippets match the EN post</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Two bilingual MDX blog posts at content/blog/view-transitions-theme-toggle.{en,pt}.mdx. Both use CodeFilename, Callout, and InlineBadge from the Phase 6 toolkit. Both reference real code from theme-toggle.tsx and globals.css.</what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open http://localhost:3000/en/blog — confirm "View Transitions API on the Theme Toggle" appears in the listing with date 2026-05-19
    2. Open http://localhost:3000/en/blog/view-transitions-theme-toggle — confirm the post renders, the CodeFilename header bar shows "src/components/shared/theme-toggle.tsx", the Callout renders with its blue info border, and InlineBadge chips are visible inline
    3. Open http://localhost:3000/pt/blog — confirm Portuguese title "View Transitions API no Alternador de Tema" appears in listing
    4. Open http://localhost:3000/pt/blog/view-transitions-theme-toggle — confirm PT prose renders correctly with the same toolkit component placements
    5. Run `pnpm build` — confirm it exits 0 (Zod schema validation passes for both files)
  </how-to-verify>
  <resume-signal>Type "approved" if both locales render correctly and pnpm build exits 0, or describe any rendering issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX body → renderer | MDX prose authored by site owner runs through next-mdx-remote@6 with blockJS: true; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | MDX body | accept | blockJS: true and blockDangerousJS: true defaults in next-mdx-remote@6 strip JS expressions and import/export; content is site-owner-only, no user input |
| T-07-02 | Information Disclosure | Code snippets | accept | Snippets are public open-source code already in the repo; no secrets or PII are quoted in the post |
</threat_model>

<verification>
- `pnpm build` exits 0 (Zod schema validation passes for both MDX files)
- `pnpm test` exits 0 (no existing tests broken)
- Both post URLs resolve to 200 (Next.js static rendering, no notFound())
- Both locale listing pages show the new post
</verification>

<success_criteria>
- content/blog/view-transitions-theme-toggle.en.mdx exists, draft: false, uses Callout + CodeFilename + InlineBadge
- content/blog/view-transitions-theme-toggle.pt.mdx exists, draft: false, idiomatic PT-BR, same toolkit usage
- pnpm build exits 0 — both files pass PostFrontmatter Zod schema
- Post appears in /en/blog and /pt/blog listings
- Post renders at /en/blog/view-transitions-theme-toggle and /pt/blog/view-transitions-theme-toggle
</success_criteria>

<output>
After completion, create .planning/phases/07-blog-post-seo-hardening/07-01-SUMMARY.md
</output>
