---
phase: 11-lighthouse-performance-fix-quality-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/case-study-hero.tsx
  - .lighthouserc.json
  - .lighthouserc.local.json
  - tests/a11y-matrix.spec.ts
  - tests/iphone-se.spec.ts
  - tests/e2e.spec.ts
autonomous: true
requirements:
  - CASE-13
  - CASE-15
  - CASE-16
  - CASE-17
user_setup: []

must_haves:
  truths:
    - "UAUBox case study page scores ≥ 0.95 on Lighthouse Performance (local and CI)"
    - "LCP is the primary contributor to the Performance score and is within budget"
    - "The mobile-first case study passes all quality gates: unit tests, Playwright E2E, a11y matrix, iPhone SE, Lighthouse ≥ 95"
    - "No regressions on existing case study pages (UAUBox, Magalu, both Machinery cases)"
  artifacts:
    - path: "src/components/sections/case-study-hero.tsx"
      provides: "Hero image with explicit width/height and tightened sizes attribute"
      contains: "width={1024}"
    - path: ".lighthouserc.json"
      provides: "CI Lighthouse config with performance threshold 0.95 and mobile-first URL"
      contains: "\"minScore\": 0.95"
    - path: ".lighthouserc.local.json"
      provides: "Local Lighthouse config with performance threshold 0.95 and mobile-first URL"
      contains: "\"minScore\": 0.95"
    - path: "tests/a11y-matrix.spec.ts"
      provides: "A11y matrix extended with mobile-first case study"
      contains: "machinery-mobile-first"
    - path: "tests/iphone-se.spec.ts"
      provides: "iPhone SE scenarios extended with mobile-first case study"
      contains: "machinery-mobile-first"
    - path: "tests/e2e.spec.ts"
      provides: "E2E test optionally extended with mobile-first variant"
      contains: "machinery-mobile-first"
  key_links:
    - from: "src/components/sections/case-study-hero.tsx"
      to: ".lighthouserc.json"
      via: "LCP fix → Lighthouse CI threshold"
      pattern: "Hero image width/height → Performance score ≥ 0.95"
    - from: "src/components/sections/case-study-hero.tsx"
      to: "tests/a11y-matrix.spec.ts"
      via: "LCP fix → a11y matrix still passes"
      pattern: "No regression on existing pages"
---

<objective>
Fix the UAUBox case study Lighthouse Performance bottleneck (LCP) and extend all quality verification test matrices to include the new mobile-first case study. Two independent workstreams: (1) hero image optimization for LCP, (2) test matrix extension.

Purpose: Closes 4 requirements (CASE-13, CASE-15, CASE-16, CASE-17). The LCP fix is a surgical 3-line change to the hero image component. The test matrix extension is a mechanical array-append across 3 test files. Both workstreams are independent and can be done in any order.

Output:
- Hero image component with explicit width/height and tightened sizes attribute
- Lighthouse CI configs with raised performance threshold (0.95) and mobile-first URL
- A11y matrix, iPhone SE, and E2E test files extended with mobile-first case study
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-lighthouse-performance-fix-quality-verification/11-RESEARCH.md
@.planning/phases/11-lighthouse-performance-fix-quality-verification/11-PATTERNS.md

<!-- Files being modified -->
@src/components/sections/case-study-hero.tsx
@.lighthouserc.json
@.lighthouserc.local.json
@tests/a11y-matrix.spec.ts
@tests/iphone-se.spec.ts
@tests/e2e.spec.ts

<!-- Reference for hero image conventions -->
@content/projects/LICENSE-images.txt

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/components/sections/case-study-hero.tsx lines 57-65 (hero image component — LCP element):
- Current: <Image src={heroSrc} alt={project.title} sizes="(max-width: 768px) 100vw, 80vw" priority fetchPriority="high" placeholder="blur" className="h-auto w-full" />
- Missing: width, height props; sizes too broad (80vw on desktop)
- Hero is inside <header className="mx-auto max-w-3xl px-4 pt-8"> (768px max width)

From .lighthouserc.json (CI config):
- Performance threshold: "categories:performance": ["error", { "minScore": 0.85 }]
- URLs: 6 URLs including /projects/uaubox-design-system
- Runs: 3, default mobile throttling (Moto G Power + slow 4G)

From .lighthouserc.local.json (local config):
- Performance threshold: "categories:performance": ["error", { "minScore": 0.8 }]
- URLs: 6 URLs on port 3001
- Runs: 1

From scripts/lh-local.sh (Lighthouse runner):
- Builds project, starts next start on port 3001, runs LHCI autorun, kills server
- Usage: pnpm lh:local (full build + check) or pnpm lh:local --skip-build (fast re-run)

From tests/a11y-matrix.spec.ts (a11y test matrix):
- Array of { path, name } objects iterated across locale/theme combos
- Current scope: 6 pages × 4 combos = 24 test cases

From tests/iphone-se.spec.ts (iPhone SE gate):
- Array of { path, name } objects
- Current scope: 6 scenarios

From tests/e2e.spec.ts (E2E tests):
- Sample case study test using /projects/magazine-luiza-superapp
- Optional: add mobile-first variant

From content/projects/LICENSE-images.txt (hero image conventions):
- All hero images: 1024×640 JPEG, 16:10 aspect ratio, saber-blue gradient
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix UAUBox LCP bottleneck</name>
  <files>src/components/sections/case-study-hero.tsx</files>
  <read_first>
    - src/components/sections/case-study-hero.tsx (current hero image component, lines 57-65)
    - content/projects/uaubox-design-system/images/hero.jpg (verify actual dimensions with `file` command)
    - content/projects/LICENSE-images.txt (hero image conventions: 1024×640 JPEG)
    - .planning/phases/11-lighthouse-performance-fix-quality-verification/11-RESEARCH.md (LCP bottleneck analysis)
    - .planning/phases/11-lighthouse-performance-fix-quality-verification/11-PATTERNS.md (Hero Image Optimization pattern)
  </read_first>
  <action>
    Apply the LCP fix to the hero image component in `src/components/sections/case-study-hero.tsx`.

    Step 1: Verify the UAUBox hero image dimensions:
    - Run `file content/projects/uaubox-design-system/images/hero.jpg`
    - Confirm dimensions match the existing convention (1024×640)
    - If dimensions differ, use the actual dimensions for `width`/`height`

    Step 2: Edit the `<Image>` component (lines 57-65) to add intrinsic dimensions and tighten `sizes`:

    Current:
    ```tsx
    <Image
      src={heroSrc}
      alt={project.title}
      sizes="(max-width: 768px) 100vw, 80vw"
      priority
      fetchPriority="high"
      placeholder="blur"
      className="h-auto w-full"
    />
    ```

    Updated:
    ```tsx
    <Image
      src={heroSrc}
      alt={project.title}
      width={1024}
      height={640}
      sizes="(max-width: 768px) 100vw, 768px"
      priority
      fetchPriority="high"
      placeholder="blur"
      className="h-auto w-full"
    />
    ```

    Changes:
    - Add `width={1024}` — intrinsic width matching the hero image file
    - Add `height={640}` — intrinsic height matching the hero image file
    - Change `sizes` from `"(max-width: 768px) 100vw, 80vw"` to `"(max-width: 768px) 100vw, 768px"` — the hero is inside `max-w-3xl` (768px), so on desktop the image is at most 768px wide, not 80vw

    Step 3: Verify the fix:
    - Run `pnpm build` to confirm no TypeScript errors
    - Run `pnpm lh:local` to verify the UAUBox Performance score improves
    - If the score is still below 0.95, check the Lighthouse report for the specific LCP contributor (it may be the hero image download time, not just layout space)

    Do NOT modify any other files in this task. The Lighthouse config threshold change is Task 2.
  </action>
  <verify>
    <automated>grep -c "width={1024}" src/components/sections/case-study-hero.tsx | awk '{ if ($1 < 1) exit 1 }' && grep -c "height={640}" src/components/sections/case-study-hero.tsx | awk '{ if ($1 < 1) exit 1 }' && grep "sizes=" src/components/sections/case-study-hero.tsx | grep -q "768px" && pnpm build 2>&1 | tail -5 | grep -E "Compiled|error" | grep -v "error" && pnpm lh:local 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/case-study-hero.tsx contains `width={1024}` on the hero `<Image>` component
    - src/components/sections/case-study-hero.tsx contains `height={640}` on the hero `<Image>` component
    - src/components/sections/case-study-hero.tsx contains `sizes="(max-width: 768px) 100vw, 768px"` (not 80vw)
    - `pnpm build` exits 0 (no TypeScript errors from the new props)
    - `pnpm lh:local` Performance score for /projects/uaubox-design-system is ≥ 0.90 (intermediate target; 0.95 is the final target after CI threshold is raised)
    - No other files were modified in this task (only case-study-hero.tsx)
    - The hero image still renders correctly (visual check: no distortion, blur placeholder works)
  </acceptance_criteria>
  <done>
    LCP bottleneck fixed. Hero image has explicit width/height and tightened sizes attribute. Lighthouse Performance score improved. Ready for Task 2 (CI threshold update) and Task 3 (test matrix extension).
  </done>
</task>

<task type="auto">
  <name>Task 2: Raise Lighthouse CI performance threshold + add mobile-first URL</name>
  <files>.lighthouserc.json, .lighthouserc.local.json</files>
  <read_first>
    - .lighthouserc.json (current performance threshold line 28, URL array lines 5-10)
    - .lighthouserc.local.json (current performance threshold line 28, URL array lines 5-10)
    - .planning/phases/11-lighthouse-performance-fix-quality-verification/11-PATTERNS.md (Lighthouse config patterns)
  </read_first>
  <action>
    Apply two coordinated edits to both Lighthouse config files.

    Edit A — .lighthouserc.json:
    - Line 28: Change `"categories:performance": ["error", { "minScore": 0.85 }]` → `"categories:performance": ["error", { "minScore": 0.95 }]`
    - Line 10: Add `"http://localhost:3000/projects/machinery-mobile-first"` to the URL array (after the uaubox-design-system URL)

    Edit B — .lighthouserc.local.json:
    - Line 28: Change `"categories:performance": ["error", { "minScore": 0.8 }]` → `"categories:performance": ["error", { "minScore": 0.95 }]`
    - Line 10: Add `"http://localhost:3001/projects/machinery-mobile-first"` to the URL array (after the uaubox-design-system URL)

    After both edits, run `pnpm lh:local` to verify the new threshold passes on all URLs including the mobile-first case study.
  </action>
  <verify>
    <automated>grep -c '"minScore": 0.95' .lighthouserc.json | awk '{ if ($1 < 1) exit 1 }' && grep -c '"minScore": 0.95' .lighthouserc.local.json | awk '{ if ($1 < 1) exit 1 }' && grep -c "machinery-mobile-first" .lighthouserc.json | awk '{ if ($1 < 1) exit 1 }' && grep -c "machinery-mobile-first" .lighthouserc.local.json | awk '{ if ($1 < 1) exit 1 }' && pnpm lh:local 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - .lighthouserc.json contains `"minScore": 0.95` for categories:performance
    - .lighthouserc.local.json contains `"minScore": 0.95` for categories:performance
    - .lighthouserc.json URL array contains `machinery-mobile-first`
    - .lighthouserc.local.json URL array contains `machinery-mobile-first`
    - `pnpm lh:local` exits 0 (all URLs pass the new 0.95 threshold)
    - No other assertions were modified (accessibility, best-practices, SEO thresholds unchanged)
  </acceptance_criteria>
  <done>
    Lighthouse CI performance threshold raised to 0.95 in both CI and local configs. Mobile-first case study URL added to both configs. All URLs pass the new threshold. CASE-13 closed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend test matrices with mobile-first case study</name>
  <files>tests/a11y-matrix.spec.ts, tests/iphone-se.spec.ts, tests/e2e.spec.ts</files>
  <read_first>
    - tests/a11y-matrix.spec.ts (current PAGES array structure)
    - tests/iphone-se.spec.ts (current scenarios array structure)
    - tests/e2e.spec.ts (current case study test pattern)
    - .planning/phases/11-lighthouse-performance-fix-quality-verification/11-PATTERNS.md (Test Matrix Extension patterns)
  </read_first>
  <action>
    Extend the three test files to include the mobile-first case study.

    Edit A — tests/a11y-matrix.spec.ts:
    - Add `{ path: '/projects/machinery-mobile-first', name: 'Machinery Mobile-First' }` to the `PAGES` array
    - This extends the a11y matrix from 6 pages × 4 combos = 24 tests to 7 pages × 4 combos = 28 tests

    Edit B — tests/iphone-se.spec.ts:
    - Add `{ path: '/projects/machinery-mobile-first', name: 'Machinery Mobile-First' }` to the `scenarios` array
    - This extends the iPhone SE gate from 6 scenarios to 7 scenarios

    Edit C — tests/e2e.spec.ts:
    - Optionally add a mobile-first variant of the case study test
    - If the existing case study test is deemed sufficient as a representative sample, skip this edit
    - If adding: mirror the existing `/projects/magazine-luiza-superapp` test structure but use `/projects/machinery-mobile-first`

    After all edits, run `pnpm test:unit` to confirm all tests pass.
  </action>
  <verify>
    <automated>grep -c "machinery-mobile-first" tests/a11y-matrix.spec.ts | awk '{ if ($1 < 1) exit 1 }' && grep -c "machinery-mobile-first" tests/iphone-se.spec.ts | awk '{ if ($1 < 1) exit 1 }' && pnpm test:unit 2>&1 | tail -10 | grep -E "passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - tests/a11y-matrix.spec.ts contains `machinery-mobile-first` in the PAGES array
    - tests/iphone-se.spec.ts contains `machinery-mobile-first` in the scenarios array
    - tests/e2e.spec.ts either contains `machinery-mobile-first` or is left unchanged (acceptable either way)
    - `pnpm test:unit` exits 0 (all existing tests still pass, no regressions)
    - `pnpm lint` exits 0 (biome check clean)
  </acceptance_criteria>
  <done>
    All test matrices extended with mobile-first case study. A11y matrix now covers 7 pages × 4 combos = 28 tests. iPhone SE gate covers 7 scenarios. All tests pass with no regressions. CASE-15 through CASE-17 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hero image dimensions → Lighthouse score | Owner-controlled. Changing `width`/`height` to match actual image dimensions is safe. Mismatched dimensions could cause visual distortion but not security issues. |
| Lighthouse CI threshold → CI pass/fail | Raising threshold from 0.85 to 0.95 is a policy decision. If the fix doesn't achieve 0.95, CI will fail. This is a safety net, not a vulnerability. |
| Test matrix extension → Test coverage | Adding new pages to test matrices increases coverage. No security impact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering / Visual regression | Hero image `width`/`height` mismatch | mitigate | Task 1 requires verifying actual image dimensions with `file` command before choosing values. All existing heroes are 1024×640. |
| T-11-02 | DoS / CI failure | Raising threshold before fix | mitigate | Task 2 is explicitly ordered after Task 1. The executor must verify the LCP fix works locally before raising the threshold. |
| T-11-03 | Information disclosure / Test data | Test matrix entries | mitigate | Test files contain only public URLs and display names. No sensitive data. |
</threat_model>

<verification>
**Plan-level verification — run after all 3 tasks complete:**

1. **Lighthouse Performance ≥ 0.95 on UAUBox:**
   - `pnpm lh:local` exits 0 with Performance score ≥ 0.95 for /projects/uaubox-design-system
   - Lighthouse report shows LCP is the primary contributor and is within budget

2. **Lighthouse CI config updated:**
   - `.lighthouserc.json` contains `"minScore": 0.95` for categories:performance
   - `.lighthouserc.local.json` contains `"minScore": 0.95` for categories:performance
   - Both configs contain `machinery-mobile-first` in URL arrays

3. **Test matrices extended:**
   - `tests/a11y-matrix.spec.ts` contains `machinery-mobile-first`
   - `tests/iphone-se.spec.ts` contains `machinery-mobile-first`
   - `pnpm test:unit` exits 0 (all tests pass)

4. **No regressions:**
   - `pnpm build` exits 0
   - `pnpm lint` exits 0
   - All existing case study pages still render correctly (visual check)

5. **Hero image still renders correctly:**
   - Visit http://localhost:3000/projects/uaubox-design-system in browser
   - Hero image displays without distortion
   - Blur placeholder works
   - No layout shift on page load
</verification>

<success_criteria>
- All 4 requirement IDs satisfied (CASE-13, CASE-15, CASE-16, CASE-17)
- `pnpm build` exits 0
- `pnpm test:unit` exits 0
- `pnpm lint` exits 0
- `pnpm lh:local` exits 0 with Performance ≥ 0.95 on /projects/uaubox-design-system
- `.lighthouserc.json` performance threshold is 0.95 (raised from 0.85)
- `.lighthouserc.local.json` performance threshold is 0.95 (raised from 0.8)
- Both Lighthouse configs include `machinery-mobile-first` URL
- `tests/a11y-matrix.spec.ts` includes mobile-first case study in PAGES array
- `tests/iphone-se.spec.ts` includes mobile-first case study in scenarios array
- Hero image in case-study-hero.tsx has explicit `width={1024}` and `height={640}`
- Hero image `sizes` tightened from `80vw` to `768px` on desktop
- No regressions on existing case study pages (UAUBox, Magalu, both Machinery cases)

**Known deltas from ROADMAP wording:**
- The Lighthouse fix targets the UAUBox case study page specifically. The mobile-first case study is expected to inherit the same LCP optimization (it uses the same CaseStudyHero component), so it should also score ≥ 0.95.
- The `width={1024} height={640}` values match the existing hero image convention (1024×640 JPEG). If the mobile-first case study uses a different hero image dimension, the same values will apply (all heroes follow the 1024×640 convention per LICENSE-images.txt).
</success_criteria>

<output>
After completion, create `.planning/phases/11-lighthouse-performance-fix-quality-verification/11-01-SUMMARY.md` covering:
- 4 requirements addressed (CASE-13, CASE-15 through CASE-17)
- LCP fix details: hero image dimensions verified, width/height added, sizes tightened
- Lighthouse scores before and after fix (local and CI)
- Test matrix extension: a11y matrix count, iPhone SE scenario count
- Build + test + lint exit codes
- Any open follow-ups for Phase 12 (if any)
</output>
