---
phase: 02-ux-polish-testing-interactions-animations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/copy-email-button.tsx
  - src/components/sections/copy-email-button.test.tsx
autonomous: true
requirements:
  - UX-08
  - UX-09

must_haves:
  truths:
    - "Clicking the copy button shows a Check icon (green) that animates in with opacity + scale over 150ms"
    - "After 2000ms the CopyIcon returns (Check exits with the same animation)"
    - "Under prefers-reduced-motion the icon swap is instant — no animation frames play"
    - "All 7 Vitest tests pass: 6 pre-existing + 1 new icon-swap test"
    - "Test 7 asserts Check renders and CopyIcon is absent after clipboard success"
  artifacts:
    - path: src/components/sections/copy-email-button.tsx
      provides: "AnimatePresence icon swap with Check + motion.span wrapper"
      contains: "AnimatePresence"
    - path: src/components/sections/copy-email-button.test.tsx
      provides: "7 tests — the 6 pre-existing + Test 7 icon-swap"
      contains: "Test 7 (icon swap)"
  key_links:
    - from: copy-email-button.tsx
      to: motion/react
      via: "AnimatePresence + motion.span wrapping Check and CopyIcon"
      pattern: "AnimatePresence.*mode=\"wait\""
    - from: copy-email-button.test.tsx
      to: lucide-react
      via: "vi.mock('lucide-react') at module level patching Check and CopyIcon with data-testid spans"
      pattern: "vi.mock\\('lucide-react'"
---

<objective>
Upgrade CopyEmailButton with an animated icon swap (CopyIcon → Check on copy success) and add the missing Test 7 that verifies the swap.

Purpose: Resolves UX-08 (test coverage gap TD-07) and UX-09 (visual polish — check icon confirmation). The button currently has no visual feedback beyond text change; the Check icon makes the success state immediately legible.
Output: Modified copy-email-button.tsx with AnimatePresence icon swap; modified copy-email-button.test.tsx with 7 passing tests.
</objective>

<execution_context>
@/Users/luizpansarini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luizpansarini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ux-polish-testing-animations/02-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/components/sections/copy-email-button.tsx (current full file):
- Props: `{ email: string; className?: string }`
- State: `const [copied, setCopied] = useState(false)`
- Current icon render (line 80): `<CopyIcon className="size-4" aria-hidden="true" />`
- Button className (line 77): `inline-flex h-11 items-center justify-center gap-2 rounded-md border border-border px-4 text-sm font-medium transition-colors hover:bg-muted focus-visible:outline-none focus-visible:ring-3 focus-visible:ring-ring/50`
- aria-live="polite" on the button element — must be preserved
- `aria-label` is NOT on the button — no changes to aria attributes
- Reset timeout: `window.setTimeout(() => setCopied(false), 2000)` — keep as-is

From src/app/[locale]/layout.tsx (relevant excerpt):
- `<MotionConfig reducedMotion="user">` wraps the entire app tree
- No per-component useReducedMotion() needed — global config handles it

From src/components/sections/copy-email-button.test.tsx (current structure):
- Top-level sonner mock at lines 7–10 (vi.mock hoisted by Vitest)
- beforeEach at lines 18–35 stubs clipboard, isSecureContext, execCommand
- 6 tests inside describe('<CopyEmailButton />', ...) — Tests 1–6
- Render helper: `import { act, render, screen } from '@/test/render'`
- TEST_EMAIL constant: `'test@example.com'`
- Test 3 pattern (lines 56–72) is the closest analog for Test 7
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AnimatePresence icon swap to CopyEmailButton</name>
  <files>src/components/sections/copy-email-button.tsx</files>
  <read_first>
    src/components/sections/copy-email-button.tsx
    src/components/ui/reveal-group.tsx
    src/app/globals.css
  </read_first>
  <behavior>
    - When copied=false: motion.span key="copy" containing CopyIcon is visible
    - When copied=true: motion.span key="check" containing Check with className="text-green-600 dark:text-green-400" is visible; motion.span key="copy" is gone from DOM
    - Icon transition: initial={{ opacity: 0, scale: 0.6 }}, animate={{ opacity: 1, scale: 1 }}, exit={{ opacity: 0, scale: 0.6 }}, transition={{ duration: 0.15, ease: 'easeOut' }}
    - Under prefers-reduced-motion: MotionConfig in layout.tsx suppresses all motion automatically; no per-component guard needed
    - The button label span (showing copied ? t('copied') : t('copyButton')) is unchanged
    - aria-live="polite" on the button element is preserved exactly
  </behavior>
  <action>
    Read copy-email-button.tsx first. Then apply these changes:

    1. Update imports: change `import { CopyIcon } from 'lucide-react'` to `import { Check, CopyIcon } from 'lucide-react'`. Add `import { AnimatePresence, motion } from 'motion/react'` before the lucide import.

    2. Replace the single `<CopyIcon className="size-4" aria-hidden="true" />` on line 80 with an AnimatePresence block. Use `mode="wait"` and `initial={false}` on AnimatePresence. Inside: render `motion.span key="check"` when copied=true, `motion.span key="copy"` when copied=false. Each motion.span gets initial/animate/exit of opacity+scale (0.6→1→0.6) and transition duration 0.15 ease 'easeOut'. The check motion.span gets className="text-green-600 dark:text-green-400". Both icons retain `className="size-4" aria-hidden="true"`.

    No `--success` CSS variable exists in globals.css (verified) — use the Tailwind fallback directly. Do not define a new CSS token. Do not add useReducedMotion() to this component — MotionConfig in layout.tsx handles it globally (per D-02, RESEARCH.md Anti-Patterns section).
  </action>
  <verify>
    <automated>cd /Users/luizpansarini/Documents/Projetos/Pessoal/pansarinitech && pnpm test src/components/sections/copy-email-button.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    copy-email-button.tsx contains AnimatePresence with mode="wait" initial={false}; contains Check and CopyIcon imports from lucide-react; contains motion.span key="check" with className including text-green-600; all 6 pre-existing tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Test 7 (icon swap) to copy-email-button.test.tsx</name>
  <files>src/components/sections/copy-email-button.test.tsx</files>
  <read_first>
    src/components/sections/copy-email-button.test.tsx
    src/components/sections/copy-email-button.tsx
  </read_first>
  <behavior>
    - Before click: getByTestId('copy-icon') is in the DOM; queryByTestId('check-icon') is null
    - After click (clipboard success): getByTestId('check-icon') is in the DOM; queryByTestId('copy-icon') is null
    - All 7 tests pass (Tests 1–6 must continue passing with the lucide mock in place)
  </behavior>
  <action>
    Read copy-email-button.test.tsx first. Then apply these two changes:

    1. Add a module-level vi.mock for lucide-react immediately after the existing sonner mock (lines 7–10). Pattern: `vi.mock('lucide-react', async (importOriginal) => { const actual = await importOriginal(); return { ...actual, Check: () => React.createElement('span', { 'data-testid': 'check-icon' }), CopyIcon: () => React.createElement('span', { 'data-testid': 'copy-icon' }) }; })`. Vitest auto-hoists vi.mock calls — place it at module scope, not inside describe. Import React at the top of the file if not already present (check first; the existing file may not import React explicitly since JSX transform handles it — use the createElement form to avoid needing the React import, or add `import React from 'react'` if needed).

    2. Append Test 7 as the last test inside the existing describe block, after Test 6. Test 7: set up clipboard success (same pattern as Test 3), render the component, assert getByTestId('copy-icon') is present and queryByTestId('check-icon') is not, click the button, then assert getByTestId('check-icon') is present and queryByTestId('copy-icon') is not. Use `userEvent.setup()` and await the click.

    Do not modify Tests 1–6 or beforeEach. The lucide mock must spread `...actual` to avoid breaking other imports from lucide-react that existing tests or the component may use.
  </action>
  <verify>
    <automated>cd /Users/luizpansarini/Documents/Projetos/Pessoal/pansarinitech && pnpm test src/components/sections/copy-email-button.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    Test output shows "7 tests | 7 passed". Test 7 (icon swap) name appears in output. queryByTestId('check-icon') and getByTestId('copy-icon') assertions both pass. No existing tests regress.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User clipboard | navigator.clipboard.writeText receives email string — same trust boundary as before; no change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | copy-email-button.tsx email prop | accept | Email is already public (rendered as visible text in Contact section); no new exposure from AnimatePresence |
| T-02-02 | Spoofing | vi.mock('lucide-react') in tests | accept | Test-only mock; does not ship to production; mock is scoped to the test file via Vitest module isolation |
</threat_model>

<verification>
Run full test suite after both tasks complete to confirm no regressions across other test files:

```
cd /Users/luizpansarini/Documents/Projetos/Pessoal/pansarinitech && pnpm test --run 2>&1 | tail -10
```

Expected: all 200 tests pass (199 pre-existing + 1 new Test 7).

Also verify copy-email-button.tsx contains exactly one AnimatePresence block and Check is imported from lucide-react:

```
grep -n "AnimatePresence\|from 'lucide-react'\|Check\b" src/components/sections/copy-email-button.tsx
```
</verification>

<success_criteria>
- copy-email-button.tsx imports AnimatePresence and motion from motion/react; imports Check and CopyIcon from lucide-react
- AnimatePresence has mode="wait" and initial={false}
- Check icon wrapped in motion.span with className containing text-green-600 dark:text-green-400
- copy-email-button.test.tsx has vi.mock('lucide-react') at module scope replacing Check and CopyIcon with data-testid spans
- Test suite reports 7 tests passing for copy-email-button.test.tsx
- Full pnpm test run exits 0 with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-ux-polish-testing-interactions-animations/02-01-SUMMARY.md`
</output>
