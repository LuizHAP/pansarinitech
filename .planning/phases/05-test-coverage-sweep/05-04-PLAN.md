---
phase: 05-test-coverage-sweep
plan: "04"
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
files_modified:
  - vitest.config.mts
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05

must_haves:
  truths:
    - "vitest.config.mts COMPONENT_FILES includes all 9 new component paths"
    - "vitest.config.mts thresholds map includes json-ld.tsx at PURE_100"
    - "personal-projects.tsx is in COMPONENT_FILES (TEST-03 config-only change)"
    - "pnpm test:coverage exits 0 with all per-file thresholds passing"
    - "CI gate runs the expanded coverage without changes to workflow YAML"
  artifacts:
    - path: "vitest.config.mts"
      provides: "Expanded COMPONENT_FILES array and PURE_100 threshold entry for json-ld.tsx"
      contains: "src/components/mdx/callout.tsx"
  key_links:
    - from: "vitest.config.mts COMPONENT_FILES"
      to: "src/components/mdx/callout.tsx (and 8 other new files)"
      via: "coverage.include spread + thresholds Object.fromEntries"
      pattern: "COMPONENT_FILES.*map.*COMPONENT_TARGET"
    - from: "vitest.config.mts thresholds"
      to: "src/components/json-ld.tsx"
      via: "explicit key in thresholds object at PURE_100"
      pattern: "json-ld\\.tsx.*PURE_100"
---

<objective>
Expand vitest.config.mts to track all 10 newly-tested components in coverage, then run pnpm test:coverage to verify the gate exits 0 with all thresholds passing.

Purpose: Fulfill TEST-03 (personal-projects config), TEST-01/02/04 (config registration), and TEST-05 (coverage gate exits 0). This is the integration step that connects the test files created in Plans 01-03 to the coverage enforcement system.
Output: Updated vitest.config.mts + passing coverage run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-test-coverage-sweep/05-CONTEXT.md
@.planning/phases/05-test-coverage-sweep/05-UI-SPEC.md
</context>

<interfaces>
<!-- Current vitest.config.mts structure. Read before editing. -->

Current LIB_DATA_FILES array (7 entries):
```typescript
const LIB_DATA_FILES = [
  'src/lib/utils.ts',
  'src/lib/seo.ts',
  'src/lib/i18n/helpers.ts',
  'src/lib/mdx/reading-time.ts',
  'src/lib/mdx/schema.ts',
  'src/lib/mdx/toc.ts',
  'src/data/schemas.ts',
]
```

Current COMPONENT_FILES array (17 entries):
```typescript
const COMPONENT_FILES = [
  'src/components/sections/hero.tsx',
  'src/components/sections/about.tsx',
  'src/components/sections/skills.tsx',
  'src/components/sections/contact.tsx',
  'src/components/sections/career-timeline.tsx',
  'src/components/sections/featured-projects-teaser.tsx',
  'src/components/sections/now-preview.tsx',
  'src/components/sections/case-study-hero.tsx',
  'src/components/sections/copy-email-button.tsx',
  'src/components/shared/header.tsx',
  'src/components/shared/footer.tsx',
  'src/components/shared/theme-toggle.tsx',
  'src/components/shared/locale-toggle.tsx',
  'src/components/shared/skip-to-content.tsx',
  'src/components/shared/easter-egg.tsx',
  'src/components/shared/theme-provider.tsx',
  'src/components/shared/command-palette.tsx',
]
```

Thresholds block:
```typescript
thresholds: {
  ...Object.fromEntries(LIB_DATA_FILES.map((f) => [f, PURE_100])),
  ...Object.fromEntries(COMPONENT_FILES.map((f) => [f, COMPONENT_TARGET])),
  'src/components/sections/hero.tsx': UNREACHABLE_NULL_COALESCE,
  'src/components/sections/contact.tsx': UNREACHABLE_NULL_COALESCE,
}
```

coverage.include:
```typescript
include: [...LIB_DATA_FILES, ...COMPONENT_FILES]
```

NOTE: json-ld.tsx goes into a new per-file threshold entry (not COMPONENT_FILES, not LIB_DATA_FILES) because it is in src/components/ but needs PURE_100. The coverage.include must list it explicitly (add alongside LIB_DATA_FILES spread), and a standalone threshold entry maps it to PURE_100.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update vitest.config.mts — add 9 COMPONENT_FILES + json-ld.tsx threshold</name>
  <files>
    vitest.config.mts
  </files>
  <read_first>
    - vitest.config.mts — current full file content before making any changes
    - .planning/phases/05-test-coverage-sweep/05-CONTEXT.md — decisions D-01 through Claude's Discretion section (which files go where)
    - .planning/phases/05-test-coverage-sweep/05-UI-SPEC.md — Vitest Configuration Contract table (all 10 files + threshold assignments)
  </read_first>
  <action>
    Edit vitest.config.mts to add the following 9 new paths to the COMPONENT_FILES array (append after 'src/components/shared/command-palette.tsx'):
      - 'src/components/mdx/callout.tsx'
      - 'src/components/mdx/note.tsx'
      - 'src/components/mdx/warning.tsx'
      - 'src/components/mdx/stat.tsx'
      - 'src/components/mdx/pre-with-copy-button.tsx'
      - 'src/components/sections/personal-projects.tsx'
      - 'src/components/blog/post-card.tsx'
      - 'src/components/blog/toc-mobile.tsx'
      - 'src/components/blog/toc-sidebar.tsx'

    For json-ld.tsx: do NOT add to COMPONENT_FILES or LIB_DATA_FILES. Instead:
      1. Add 'src/components/json-ld.tsx' to the coverage.include array as a standalone entry (e.g. include: [...LIB_DATA_FILES, ...COMPONENT_FILES, 'src/components/json-ld.tsx']).
      2. Add the explicit threshold entry in the thresholds object: 'src/components/json-ld.tsx': PURE_100, placed after the UNREACHABLE_NULL_COALESCE overrides for clarity.

    Do not change any existing entries, threshold constants, or project configurations. The only changes are: COMPONENT_FILES gains 9 entries, coverage.include gains 1 explicit path, thresholds gains 1 explicit key.
  </action>
  <verify>
    <automated>cd /Users/luizpansarini/Documents/Projetos/Pessoal/pansarinitech && grep -c "src/components/mdx/callout.tsx" vitest.config.mts && grep -c "src/components/json-ld.tsx" vitest.config.mts && grep -c "src/components/sections/personal-projects.tsx" vitest.config.mts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'src/components/mdx/callout.tsx' vitest.config.mts outputs 1 (appears in COMPONENT_FILES)
    - grep -c 'src/components/json-ld.tsx' vitest.config.mts outputs 2 (appears in coverage.include AND in thresholds)
    - grep -c 'src/components/sections/personal-projects.tsx' vitest.config.mts outputs 1 (appears in COMPONENT_FILES)
    - grep -c 'src/components/blog/toc-sidebar.tsx' vitest.config.mts outputs 1 (appears in COMPONENT_FILES)
    - COMPONENT_FILES array has 26 entries (17 original + 9 new)
    - Existing entries (hero.tsx, contact.tsx UNREACHABLE_NULL_COALESCE overrides) are unchanged
  </acceptance_criteria>
  <done>vitest.config.mts contains all 10 new file paths. COMPONENT_FILES has 26 entries. json-ld.tsx appears twice (include array + thresholds). No TypeScript errors when running pnpm tsc --noEmit on vitest.config.mts.</done>
</task>

<task type="auto">
  <name>Task 2: Run pnpm test:coverage — verify gate exits 0</name>
  <files></files>
  <read_first>
    - vitest.config.mts — confirm all 10 new files are present before running (Task 1 must be complete)
  </read_first>
  <action>
    Run pnpm test:coverage. All per-file thresholds for the 10 new files must pass:
      - 9 new COMPONENT_FILES at 70/60/70/70 (statements/branches/functions/lines)
      - src/components/json-ld.tsx at 100/100/100/100

    If any threshold fails, diagnose which file and which metric, then check:
      1. If the test file from Plans 01-03 is missing a test variant that exercises a branch → add that variant
      2. If it is a structurally unreachable branch (like the null-coalesce pattern) → add an UNREACHABLE_* override constant following the existing pattern

    Do NOT lower thresholds below the values specified in CONTEXT.md decisions. The 60% branch floor for COMPONENT_TARGET is already accommodating for client components with hard-to-reach clipboard/secure-context branches.
  </action>
  <verify>
    <automated>cd /Users/luizpansarini/Documents/Projetos/Pessoal/pansarinitech && pnpm test:coverage 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test:coverage exits with code 0
    - Terminal output contains no lines matching "does not meet" or "threshold" violation patterns
    - All previously passing 200+ tests still pass (zero regressions)
    - Coverage report generated in ./coverage directory
  </acceptance_criteria>
  <done>pnpm test:coverage exits 0. All per-file thresholds for all 27 tracked files (17 original + 9 new COMPONENT_FILES + json-ld.tsx) pass. Zero regressions in existing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI coverage gate → merge | Coverage gate blocks merge on threshold regression — no trust boundary change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | vitest.config.mts threshold lowering | accept | Thresholds are code-reviewed in PRs; accidental lowering caught by diff review |
</threat_model>

<verification>
pnpm test:coverage exits 0. grep -c 'src/components/json-ld.tsx' vitest.config.mts returns 2. grep -c 'src/components/sections/personal-projects.tsx' vitest.config.mts returns 1.
</verification>

<success_criteria>
- vitest.config.mts COMPONENT_FILES array contains all 9 new component paths
- vitest.config.mts coverage.include contains 'src/components/json-ld.tsx' as an explicit entry
- vitest.config.mts thresholds map contains 'src/components/json-ld.tsx': PURE_100
- pnpm test:coverage exits 0 with no threshold violations across all 27 tracked files
- All pre-existing 200+ tests still pass (zero regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-sweep/05-04-SUMMARY.md`
</output>
