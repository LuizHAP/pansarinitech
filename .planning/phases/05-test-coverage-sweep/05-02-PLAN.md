---
phase: 05-test-coverage-sweep
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/json-ld.test.tsx
autonomous: true
requirements:
  - TEST-02

must_haves:
  truths:
    - "json-ld.test.tsx exists and passes in the jsdom project with 4 tests"
    - "Test 1 asserts script type=application/ld+json and innerHTML matches JSON.stringify(schema)"
    - "Test 2 asserts < in schema values is escaped to \\u003c in rendered HTML"
    - "Test 3 asserts AUTHOR_PERSON['@type'] === 'Person' and AUTHOR_PERSON.name === 'Luiz Pansarini'"
    - "Test 4 asserts SITE_URL is a non-empty string"
  artifacts:
    - path: "src/components/json-ld.test.tsx"
      provides: "4 tests covering script output, XSS escape, and exported constant shapes"
      exports: []
  key_links:
    - from: "src/components/json-ld.test.ts"
      to: "src/components/json-ld.tsx"
      via: "direct import, raw @testing-library/react render"
      pattern: "JSON\\.stringify.*replace.*\\\\u003c"
---

<objective>
Write the Vitest test file for json-ld.tsx. This file is a pure-logic RSC that serializes schema objects to a script tag — it runs in the node project and must reach 100/100/100/100 coverage.

Purpose: Fulfill TEST-02. json-ld.tsx has deterministic output with no async, no context — raw render is sufficient.
Output: src/components/json-ld.test.ts (note: .ts extension — no JSX needed for node render assertions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-test-coverage-sweep/05-CONTEXT.md
@.planning/phases/05-test-coverage-sweep/05-UI-SPEC.md
</context>

<interfaces>
<!-- Key signatures from src/components/json-ld.tsx -->

```
export { SITE_URL } from '@/lib/seo'
// SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://pansarinitech.vercel.app'

export const AUTHOR_PERSON = { '@type': 'Person', name: 'Luiz Pansarini' } as const

interface JsonLdProps { schema: Record<string, unknown> }

export default function JsonLd({ schema }: JsonLdProps)
// Returns: <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema).replace(/</g, '\\u003c') }} />
```

From vitest.config.mts node project:
```
// node project include pattern: 'src/lib/**/*.test.ts', 'src/data/**/*.test.ts'
// json-ld.test.ts is in src/components/ — it must be placed as src/components/json-ld.test.ts
// and the jsdom project include pattern 'src/components/**/*.test.ts' will pick it up.
// However, json-ld.tsx uses dangerouslySetInnerHTML with React — render() needs jsdom OR
// we can assert innerHTML via document.createElement in node. BUT: per CONTEXT D-04,
// use raw @testing-library/react (which requires jsdom). So the test file uses .test.ts
// extension and runs under the jsdom project. Coverage still tracked at PURE_100.
```

IMPORTANT: json-ld.tsx imports React and returns JSX. Testing in node environment requires
either JSDOM or asserting without rendering. Per D-04 in CONTEXT.md, use raw
@testing-library/react render (jsdom). The file extension .test.tsx is correct.
The coverage threshold (PURE_100) is independent of which project runs the test.

From src/components/shared/theme-provider.test.tsx — raw RTL render pattern:
```typescript
import { render } from '@testing-library/react'
// No wrapper, no locale context
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: json-ld.test.tsx — script output, XSS escape, and exported constants</name>
  <files>
    src/components/json-ld.test.tsx
  </files>
  <read_first>
    - src/components/json-ld.tsx — full implementation: default export JsonLd, AUTHOR_PERSON const, SITE_URL re-export, dangerouslySetInnerHTML pattern
    - src/lib/seo.ts — SITE_URL definition (process.env.NEXT_PUBLIC_SITE_URL ?? 'https://pansarinitech.vercel.app')
    - src/components/shared/theme-provider.test.tsx — raw @testing-library/react render pattern without wrapper
    - .planning/phases/05-test-coverage-sweep/05-UI-SPEC.md — TEST-02 section for exact assertion requirements
  </read_first>
  <behavior>
    Test 1 (script output): render(<JsonLd schema={{ '@type': 'Article', name: 'Test' }} />) → container.querySelector('script[type="application/ld+json"]') is not null → its innerHTML equals JSON.stringify({ '@type': 'Article', name: 'Test' })
    Test 2 (XSS escape): render(<JsonLd schema={{ title: '<script>alert(1)</script>' }} />) → container.querySelector('script[type="application/ld+json"]').innerHTML does NOT contain raw '<' character → DOES contain '\\u003c'
    Test 3 (AUTHOR_PERSON): AUTHOR_PERSON['@type'] === 'Person' AND AUTHOR_PERSON.name === 'Luiz Pansarini'
    Test 4 (SITE_URL): typeof SITE_URL === 'string' AND SITE_URL.length > 0
  </behavior>
  <action>
    Create src/components/json-ld.test.tsx. Import render from '@testing-library/react' (NOT from '@/test/render' — no locale wrapper needed). Import JsonLd as default, AUTHOR_PERSON, and SITE_URL from './json-ld'.

    Test 1: Call render(<JsonLd schema={{ '@type': 'Article', name: 'Test' }} />). Select script element via container.querySelector('script[type="application/ld+json"]'). Assert it is not null. Assert its innerHTML equals JSON.stringify({ '@type': 'Article', name: 'Test' }) — no XSS replacement needed since no '<' in this schema.

    Test 2 (XSS): Render with schema containing '<script>alert(1)</script>' as a value. Assert the script element's innerHTML does NOT include the literal string '<' (raw less-than character). Assert it DOES include the string '\\u003c'. This documents that the replace(/</g, '\\u003c') is functioning as a security boundary.

    Test 3: Assert AUTHOR_PERSON['@type'] === 'Person' and AUTHOR_PERSON.name === 'Luiz Pansarini' using toBe().

    Test 4: Assert typeof SITE_URL === 'string' using toBe() and SITE_URL.length > 0 using toBeGreaterThan(0).

    File extension MUST be .test.tsx (contains JSX). The jsdom project picks it up via 'src/components/**/*.test.tsx'. Coverage is registered in the thresholds map at PURE_100 — that configuration happens in Plan 05-04.
  </action>
  <verify>
    <automated>cd /Users/luizpansarini/Documents/Projetos/Pessoal/pansarinitech && pnpm vitest run src/components/json-ld.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>4 tests pass: script type assertion, XSS escape assertion (innerHTML lacks raw '<', contains '\\u003c'), AUTHOR_PERSON shape assertion, SITE_URL non-empty string assertion. Zero TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema input → dangerouslySetInnerHTML | JsonLd serializes schema objects; XSS test documents the < escape boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Injection (XSS) | json-ld.tsx script output | mitigate | Test 2 explicitly asserts that < in schema values is replaced with < — documents and validates the security boundary established in Phase 4 |
</threat_model>

<verification>
pnpm vitest run src/components/json-ld.test.tsx exits 0 with 4 passing tests.
</verification>

<success_criteria>
- src/components/json-ld.test.tsx exists with 4 tests
- All 4 tests pass in the jsdom project
- Test 2 explicitly asserts the < → < escape (security boundary documented)
- AUTHOR_PERSON and SITE_URL export shape verified
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-sweep/05-02-SUMMARY.md`
</output>
