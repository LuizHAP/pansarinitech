---
phase: 13-js-bundle-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/types.ts
  - src/data/schemas.ts
  - src/lib/i18n/helpers.ts
  - src/components/shared/deferred-command-palette.tsx
  - src/components/shared/header.tsx
autonomous: true
requirements:
  - CASE-24
  - CASE-25
  - CASE-26
user_setup: []

must_haves:
  truths:
    - "Total JS transfer size reduced by ≥ 50% (608KB → ≤ 300KB target)"
    - "Lighthouse Performance score improves (current CI: 0.75, target: ≥ 0.85)"
    - "No regressions in functionality (all interactive elements still work)"
    - "No regressions in tests (unit, e2e, a11y all pass)"
  artifacts:
    - path: "src/data/types.ts"
      provides: "Zod-free TypeScript types extracted from schemas"
      contains: "export type Role"
    - path: "src/data/schemas.ts"
      provides: "Zod schemas only, no type exports"
      contains: "import { z } from 'zod'"
    - path: "src/lib/i18n/helpers.ts"
      provides: "Import updated from @/data/schemas to @/data/types"
      contains: "from '@/data/types'"
    - path: "src/components/shared/deferred-command-palette.tsx"
      provides: "Client wrapper for dynamic CommandPaletteRoot import"
      contains: "ssr: false"
    - path: "src/components/shared/header.tsx"
      provides: "Uses deferred palette instead of direct import"
      contains: "from './deferred-command-palette'"
  key_links:
    - from: "src/lib/i18n/helpers.ts"
      to: "src/data/types.ts"
      via: "type import"
      pattern: "import type { Role } from '@/data/types'"
    - from: "src/components/shared/header.tsx"
      to: "src/components/shared/deferred-command-palette.tsx"
      via: "dynamic import"
      pattern: "CommandPaletteRoot deferred via next/dynamic"
---

<objective>
Reduce the 608KB JS bundle by eliminating Zod from the client bundle and deferring the command palette. Two independent optimizations: (1) split types from schemas to break the Zod → client chain, (2) defer CommandPaletteRoot hydration until first Cmd+K press.

Purpose: Closes 3 requirements (CASE-24, CASE-25, CASE-26). The Zod split is a surgical 3-file change. The command palette defer is a dynamic import change. Both are independent and can be done in any order.

Output:
- New `src/data/types.ts` with Zod-free TypeScript types
- Modified `src/data/schemas.ts` with only Zod schemas (no type exports)
- Updated `src/lib/i18n/helpers.ts` importing from `@/data/types`
- Reused `src/components/shared/deferred-command-palette.tsx` (from Phase 11)
- Updated `src/components/shared/header.tsx` using deferred palette
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-js-bundle-optimization/13-RESEARCH.md
@.planning/phases/13-js-bundle-optimization/13-PATTERNS.md

<!-- Files being modified -->
@src/data/types.ts (new)
@src/data/schemas.ts
@src/lib/i18n/helpers.ts
@src/components/shared/deferred-command-palette.tsx
@src/components/shared/header.tsx

<!-- Reference for Zod chunk analysis -->
@.next/static/chunks/0erd080oddvxq.js (Zod — 226KB)

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/data/schemas.ts (current Zod schemas + type exports):
- Exports: Role, SkillCategory, SkillLevel, Project, BlogPost types
- Exports: roleSchema, skillCategorySchema, skillLevelSchema, projectSchema, blogPostSchema Zod schemas
- All types are inferred from Zod schemas via z.infer<>

From src/lib/i18n/helpers.ts (current import):
- Line 8: import type { Role } from '@/data/schemas';
- Role is used in getRoleLabel() function

From src/components/shared/command-palette.tsx (current imports):
- Imports 10 lucide icons: Search, BookOpen, FileText, Code2, Briefcase, GraduationCap, Mail, Moon, Sun, Globe
- Imports shadcn/ui components: Command, Input, Dialog, Separator, Label, Badge, Tooltip
- All hydrate on every page load but only needed on Cmd+K

From src/components/shared/header.tsx (current import):
- Line 12: import { CommandPaletteRoot } from './command-palette';
- Renders CommandPaletteRoot directly (no deferral)

From src/components/shared/deferred-command-palette.tsx (Phase 11 artifact):
- May exist or may have been deleted during Phase 11 investigation
- If missing, recreate with: 'use client' wrapper + dynamic import + ssr: false

From .next/static/chunks/ (bundle analysis):
- 0erd080oddvxq.js — 226KB (Zod) — ELIMINABLE
- 09i1i0u87fy9c.js — 190KB (lucide + shadcn) — DEFERRABLE
- 11.00uwk2r_mi.js — 288KB (React/Next.js) — FIXED COST
- 11be-bqh67r_l.js — 282KB (React/Next.js) — FIXED COST
- 0qnm4tx4gf8s6.js — 282KB (React/Next.js) — FIXED COST
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split types from schemas (break Zod → client chain)</name>
  <files>src/data/types.ts (new), src/data/schemas.ts (modify), src/lib/i18n/helpers.ts (modify)</files>
  <read_first>
    - src/data/schemas.ts (current Zod schemas + type exports)
    - src/lib/i18n/helpers.ts (current import from @/data/schemas)
    - .planning/phases/13-js-bundle-optimization/13-PATTERNS.md (Type Extraction pattern)
    - .planning/phases/13-js-bundle-optimization/13-RESEARCH.md (Pattern 1: Extract Zod-Free Types)
  </read_first>
  <action>
    Split TypeScript types from Zod schemas to break the Zod → client bundle chain.

    Step 1: Create `src/data/types.ts` with all type definitions (no Zod imports):
    ```typescript
    // src/data/types.ts (NEW)
    export type Role = 'principal' | 'senior' | 'mid' | 'junior';
    export type SkillCategory = 'frontend' | 'backend' | 'devops' | 'mobile' | 'design';
    export type SkillLevel = 'expert' | 'advanced' | 'intermediate' | 'beginner';
    export type Project = {
      title: string;
      role: string;
      year: number;
      stack: string[];
      blurb: string;
      heroImage: string;
      tags: string[];
      featured: boolean;
      order: number;
    };
    export type BlogPost = {
      title: string;
      date: string;
      excerpt: string;
      tags: string[];
      slug: string;
      readingTime: number;
    };
    ```

    Step 2: Modify `src/data/schemas.ts` — remove all type exports, keep only Zod schemas:
    - Remove: `export type Role = ...`
    - Remove: `export type SkillCategory = ...`
    - Remove: `export type SkillLevel = ...`
    - Remove: `export type Project = ...`
    - Remove: `export type BlogPost = ...`
    - Keep: All Zod schemas (roleSchema, skillCategorySchema, etc.)
    - Keep: `import { z } from 'zod'`

    Step 3: Modify `src/lib/i18n/helpers.ts` — update import path:
    - Change: `import type { Role } from '@/data/schemas';`
    - To: `import type { Role } from '@/data/types';`

    Step 4: Verify no other files import types from `@/data/schemas`:
    - Run: `grep -r "from ['\"]@/data/schemas['\"]" src/ --include="*.ts" --include="*.tsx"`
    - If any results, update those imports to use `@/data/types` instead

    Step 5: Run `pnpm build` to verify no TypeScript errors.
  </action>
  <verify>
    <automated>test -f src/data/types.ts && grep -c "export type Role" src/data/types.ts | awk '{ if ($1 < 1) exit 1 }' && grep -c "export type" src/data/schemas.ts | awk '{ if ($1 > 0) exit 1 }' && grep -c "from '@/data/types'" src/lib/i18n/helpers.ts | awk '{ if ($1 < 1) exit 1 }' && pnpm build 2>&1 | tail -5 | grep -E "Compiled|error" | grep -v "error"</automated>
  </verify>
  <acceptance_criteria>
    - File src/data/types.ts exists with all type definitions (Role, SkillCategory, SkillLevel, Project, BlogPost)
    - src/data/types.ts has NO Zod imports
    - src/data/schemas.ts has NO type exports (only Zod schemas)
    - src/lib/i18n/helpers.ts imports Role from '@/data/types' (not '@/data/schemas')
    - No other files import types from '@/data/schemas' (only Zod schemas)
    - pnpm build exits 0 (no TypeScript errors)
    - Zod chunk (0erd080oddvxq.js) should no longer be in the client bundle
  </acceptance_criteria>
  <done>
    Types split from schemas. Zod is now server-side only. The Zod → client bundle chain is broken. Ready for Task 2 (defer command palette).
  </done>
</task>

<task type="auto">
  <name>Task 2: Defer CommandPaletteRoot hydration</name>
  <files>src/components/shared/deferred-command-palette.tsx, src/components/shared/header.tsx</files>
  <read_first>
    - src/components/shared/command-palette.tsx (current CommandPaletteRoot component)
    - src/components/shared/header.tsx (current import of CommandPaletteRoot)
    - .planning/phases/13-js-bundle-optimization/13-PATTERNS.md (Dynamic Import pattern)
    - .planning/phases/13-js-bundle-optimization/13-RESEARCH.md (Pattern 2: Dynamic Import for Heavy Client Components)
  </read_first>
  <action>
    Defer CommandPaletteRoot hydration until first Cmd+K press using next/dynamic.

    Step 1: Ensure `src/components/shared/deferred-command-palette.tsx` exists:
    - If the file was deleted during Phase 11 investigation, recreate it:
    ```typescript
    // src/components/shared/deferred-command-palette.tsx
    'use client';
    import dynamic from 'next/dynamic';
    const CommandPaletteRoot = dynamic(
      () => import('./command-palette').then((mod) => mod.CommandPaletteRoot),
      { ssr: false },
    );
    export { CommandPaletteRoot };
    ```

    Step 2: Modify `src/components/shared/header.tsx`:
    - Change: `import { CommandPaletteRoot } from './command-palette';`
    - To: `import { CommandPaletteRoot } from './deferred-command-palette';`

    Step 3: Run `pnpm build` to verify no TypeScript errors.
    Step 4: Run `pnpm test:unit` to verify no regressions.
  </action>
  <verify>
    <automated>test -f src/components/shared/deferred-command-palette.tsx && grep -c "ssr: false" src/components/shared/deferred-command-palette.tsx | awk '{ if ($1 < 1) exit 1 }' && grep -c "from './deferred-command-palette'" src/components/shared/header.tsx | awk '{ if ($1 < 1) exit 1 }' && pnpm build 2>&1 | tail -5 | grep -E "Compiled|error" | grep -v "error" && pnpm test:unit 2>&1 | tail -5 | grep -E "passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/shared/deferred-command-palette.tsx exists
    - Contains 'use client' directive
    - Uses next/dynamic with ssr: false
    - src/components/shared/header.tsx imports from './deferred-command-palette' (not './command-palette')
    - pnpm build exits 0 (no TypeScript errors)
    - pnpm test:unit exits 0 (no regressions)
    - Command palette still opens on Cmd+K (manual verification)
  </acceptance_criteria>
  <done>
    CommandPaletteRoot is now deferred. The 10 lucide icons + shadcn/ui bundle only loads on first Cmd+K press. Both optimizations complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type extraction | Types are owner-authored, no user input. Safe to extract. |
| Dynamic import | next/dynamic with ssr: false is a standard Next.js pattern. No security impact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering / Type mismatch | Splitting types from schemas | mitigate | TypeScript compiler catches any type mismatches at build time. pnpm build must pass. |
| T-13-02 | DoS / Missing component | Deferred command palette not loading | mitigate | ssr: false means the component renders null on server, loads on client. If dynamic import fails, the header still renders (just without the palette). |
</threat_model>

<verification>
**Plan-level verification — run after all 2 tasks complete:**

1. **Bundle size reduced:**
   - `ls -la .next/static/chunks/ | awk '{sum += $5} END {print sum}'` — total should be ≤ 300KB
   - `grep -c "zod" .next/static/chunks/*.js` — should return 0 (Zod not in any chunk)

2. **Lighthouse Performance improves:**
   - `pnpm lh:local` — Performance score should be ≥ 0.85 (up from 0.75)

3. **No regressions:**
   - `pnpm build` exits 0
   - `pnpm test:unit` exits 0
   - `pnpm lint` exits 0
   - Command palette still opens on Cmd+K (manual verification)

4. **Zod is server-side only:**
   - `grep -r "from ['\"]zod['\"]" src/ --include="*.ts" --include="*.tsx"` — only 3 results (schema.ts, factory.ts, schemas.ts)
   - No client component imports from @/data/schemas
</verification>

<success_criteria>
- All 3 requirement IDs satisfied (CASE-24, CASE-25, CASE-26)
- `pnpm build` exits 0
- `pnpm test:unit` exits 0
- `pnpm lint` exits 0
- Total JS bundle size reduced by ≥ 50% (608KB → ≤ 300KB)
- Lighthouse Performance score improves (current CI: 0.75, target: ≥ 0.85)
- Zod is not in any client-side chunk
- Command palette still works (deferred, not removed)
- No regressions in functionality or tests
</success_criteria>

<output>
After completion, create `.planning/phases/13-js-bundle-optimization/13-01-SUMMARY.md` covering:
- 3 requirements addressed (CASE-24 through CASE-26)
- Bundle size before and after (total KB, Zod chunk eliminated)
- Lighthouse Performance score before and after
- Build + test + lint exit codes
- Any open follow-ups for Phase 14 (New Blog Post) or Phase 15 (New Case Study)
</output>
