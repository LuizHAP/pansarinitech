---
phase: 06-mdx-component-toolkit-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/components/mdx/code-filename.test.tsx
  - src/components/mdx/inline-badge.test.tsx
  - vitest.config.mts
autonomous: true
requirements:
  - MDX-05

must_haves:
  truths:
    - "pnpm test:unit exits 0 with code-filename.test.tsx and inline-badge.test.tsx in the passing suite"
    - "pnpm test:coverage exits 0 with both new component files tracked at COMPONENT_TARGET (70/60/70/70) thresholds"
    - "CodeFilename async RSC tests verify: filename string rendered, FileIcon svg present with aria-hidden, outer div aria-label equals 'File: src/app/page.tsx'"
    - "InlineBadge sync RSC tests verify: children rendered, each of the 4 variants applies its correct className tokens, default variant is primary"
  artifacts:
    - path: "src/components/mdx/code-filename.test.tsx"
      provides: "Async RSC test for CodeFilename using vi.mock('next-intl/server') pattern"
      exports: []
    - path: "src/components/mdx/inline-badge.test.tsx"
      provides: "Sync RSC test for InlineBadge with 4 variant assertions"
      exports: []
    - path: "vitest.config.mts"
      provides: "Updated COMPONENT_FILES array tracking both new component files"
      contains: "code-filename.tsx, inline-badge.tsx"
  key_links:
    - from: "vitest.config.mts COMPONENT_FILES"
      to: "src/components/mdx/code-filename.tsx"
      via: "coverage include list"
      pattern: "code-filename\\.tsx"
    - from: "vitest.config.mts COMPONENT_FILES"
      to: "src/components/mdx/inline-badge.tsx"
      via: "coverage include list"
      pattern: "inline-badge\\.tsx"
---

<objective>
Write Vitest tests for `CodeFilename` (async RSC) and `InlineBadge` (sync RSC), then add both component files to the `COMPONENT_FILES` array in `vitest.config.mts` at `COMPONENT_TARGET` (70/60/70/70) thresholds. This satisfies MDX-05 and ensures both new components are covered from day one.

Purpose: Phase 5 established the coverage baseline; Phase 6 must not shrink it. New components enter coverage scope immediately, not retroactively.
Output: `code-filename.test.tsx`, `inline-badge.test.tsx`, updated `vitest.config.mts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-mdx-component-toolkit-expansion/06-CONTEXT.md
@.planning/phases/06-mdx-component-toolkit-expansion/06-RESEARCH.md
@.planning/phases/06-mdx-component-toolkit-expansion/06-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/mdx/callout.test.tsx (async RSC mock pattern — reference for code-filename.test.tsx):
The test file uses vi.mock('next-intl/server', ...) before imports. It then:
  1. vi.mock placed before component import (Vitest hoists it anyway)
  2. In each test: const jsx = await Callout({ type: 'info', children: 'content' })
  3. const { getByRole, getByText } = render(jsx)
  4. Assertions on the rendered DOM

From src/components/mdx/stat.test.tsx (sync RSC pattern — reference for inline-badge.test.tsx):
  1. No vi.mock needed
  2. const { container } = render(Stat({ number: '47', label: 'engineers onboarded' }))
  3. const span = container.querySelector('.text-4xl')
  4. expect(span).toBeInTheDocument()

From vitest.config.mts (COMPONENT_FILES array and COMPONENT_TARGET constant):
  COMPONENT_TARGET = { statements: 70, branches: 60, functions: 70, lines: 70 }
  COMPONENT_FILES is a string[] — new entries are: 'src/components/mdx/code-filename.tsx' and 'src/components/mdx/inline-badge.tsx'
  The coverage thresholds block uses Object.fromEntries(COMPONENT_FILES.map((f) => [f, COMPONENT_TARGET]))
  so adding to COMPONENT_FILES automatically applies COMPONENT_TARGET — no manual threshold entry needed.

From src/components/mdx/code-filename.tsx (created in Plan 01 — read actual file):
  - export async function CodeFilename({ filename, children }: { filename: string; children: ReactNode })
  - Outer div: className contains 'rounded-md', 'overflow-hidden', 'border-border'
  - aria-label: `${t('codeFilename.ariaPrefix')}: ${filename}`
  - FileIcon: aria-hidden="true", className contains 'size-3.5'
  - Filename span: className contains 'font-mono', 'text-muted-foreground'

From src/components/mdx/inline-badge.tsx (created in Plan 01 — read actual file):
  - export function InlineBadge({ variant = 'primary', children }: { variant?: BadgeVariant; children: ReactNode })
  - VARIANT_STYLES: { primary: 'border-primary/30 text-primary', secondary: 'bg-muted text-muted-foreground', muted: 'border-border text-muted-foreground', destructive: 'border-destructive/30 text-destructive' }
  - Renders a <span> with the combined className
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write CodeFilename tests (async RSC mock pattern)</name>
  <files>src/components/mdx/code-filename.test.tsx</files>
  <read_first>
    - src/components/mdx/callout.test.tsx — the exact async RSC vi.mock pattern to replicate (vi.mock placement, getLocale mock, getTranslations mock returning a function keyed by string, awaiting the component call)
    - src/components/mdx/code-filename.tsx — the actual component created in Plan 01 (read to confirm exact class names, element structure, and aria-label format before writing assertions)
  </read_first>
  <action>
    Create src/components/mdx/code-filename.test.tsx. Structure:

    1. Import React from 'react', describe/expect/it/vi from 'vitest'.

    2. vi.mock block (before component import — Vitest hoists regardless, but place before for readability):
       - Mock 'next-intl/server' with: getLocale returning 'en', getTranslations returning a function that maps 'codeFilename.ariaPrefix' → 'File' and falls back to returning the key for anything else.

    3. Import { render } from '@testing-library/react' and { CodeFilename } from './code-filename'.

    4. describe('<CodeFilename />') with these test cases:
       a. "renders filename string in the filename bar" — await CodeFilename({ filename: 'src/app/page.tsx', children: React.createElement('pre', null, 'code') }), render result, expect getByText('src/app/page.tsx') to be in the document.
       b. "renders FileIcon as aria-hidden svg" — same setup, check container.querySelector('svg[aria-hidden="true"]') is not null.
       c. "outer div has aria-label with translated prefix and filename" — same setup, get container.firstChild as HTMLElement, expect getAttribute('aria-label') to equal 'File: src/app/page.tsx'.
       d. "renders children inside the container" — pass children: React.createElement('pre', { 'data-testid': 'child-pre' }, 'code'), expect getByTestId('child-pre') to be in the document.

    Key constraint: DO NOT use renderWithLocale (InlineBadge has no locale dep — this is CodeFilename but both new components are RSC, not client). Use raw render() from @testing-library/react after awaiting the async component call.
  </action>
  <verify>
    <automated>pnpm test:unit --reporter=verbose 2>&1 | grep -E "code-filename"</automated>
  </verify>
  <done>
    - src/components/mdx/code-filename.test.tsx exists with at least 4 test cases in a describe block
    - All 4 tests pass (pnpm test:unit exits 0)
    - Tests cover: filename text rendered, FileIcon aria-hidden, aria-label format, children passthrough
  </done>
</task>

<task type="auto">
  <name>Task 2: Write InlineBadge tests and expand vitest.config.mts</name>
  <files>
    src/components/mdx/inline-badge.test.tsx
    vitest.config.mts
  </files>
  <read_first>
    - src/components/mdx/stat.test.tsx — the sync RSC test pattern to replicate (no vi.mock, render(Component({...})), container.querySelector assertions)
    - src/components/mdx/inline-badge.tsx — the actual component from Plan 01 (read to confirm exact className strings for each variant before writing assertions)
    - vitest.config.mts — the current COMPONENT_FILES array (to see where to append the two new entries without breaking the existing structure)
  </read_first>
  <action>
    Create src/components/mdx/inline-badge.test.tsx. Structure:

    1. Import React from 'react', describe/expect/it from 'vitest'.
    2. Import { render } from '@testing-library/react' and { InlineBadge } from './inline-badge'.
    3. No vi.mock needed — InlineBadge is pure sync with no next-intl dependency.
    4. describe('<InlineBadge />') with these test cases:
       a. "renders children text" — render(InlineBadge({ children: 'hello' })), expect getByText('hello') to be in the document.
       b. "default variant is primary — applies text-primary and border-primary classes" — render(InlineBadge({ children: 'label' })), const span = container.querySelector('span'), expect span?.className to contain 'text-primary' and to contain 'border-primary'.
       c. "secondary variant applies bg-muted and text-muted-foreground" — render(InlineBadge({ variant: 'secondary', children: 'label' })), expect span?.className to contain 'bg-muted' and 'text-muted-foreground'.
       d. "muted variant applies text-muted-foreground and border-border" — render(InlineBadge({ variant: 'muted', children: 'label' })), expect span?.className to contain 'text-muted-foreground' and 'border-border'.
       e. "destructive variant applies text-destructive and border-destructive" — render(InlineBadge({ variant: 'destructive', children: 'label' })), expect span?.className to contain 'text-destructive' and 'border-destructive'.
       f. "renders a span element (not a div)" — expect container.querySelector('span') to not be null, expect container.querySelector('div') to be null.

    Update vitest.config.mts: in the COMPONENT_FILES array, append two new entries after the last existing MDX entry ('src/components/mdx/pre-with-copy-button.tsx' or the last entry in that group):
       'src/components/mdx/code-filename.tsx',
       'src/components/mdx/inline-badge.tsx',
    Both use COMPONENT_TARGET via the Object.fromEntries spread — no manual threshold override needed. Do not modify the thresholds block.
  </action>
  <verify>
    <automated>pnpm test:coverage 2>&1 | tail -30</automated>
  </verify>
  <done>
    - src/components/mdx/inline-badge.test.tsx exists with at least 6 test cases covering all 4 variants plus children and element type
    - 'src/components/mdx/code-filename.tsx' and 'src/components/mdx/inline-badge.tsx' appear in COMPONENT_FILES in vitest.config.mts
    - pnpm test:unit exits 0 (all tests including the two new test files passing)
    - pnpm test:coverage exits 0 with both new files meeting COMPONENT_TARGET (70/60/70/70) thresholds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mock → component | vi.mock('next-intl/server') replaces the real module; mock must return a function for getTranslations (not an object) to match the component's usage pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | vitest.config.mts COMPONENT_FILES | accept | Config file is owner-controlled; adding to COMPONENT_FILES only increases coverage scope (never reduces it); threshold is set by COMPONENT_TARGET constant |
| T-06-05 | Repudiation | Test suite coverage gate | mitigate | pnpm test:coverage in CI blocks merge if either new file falls below COMPONENT_TARGET — coverage is the audit trail |
</threat_model>

<verification>
After both tasks complete:

```bash
# All unit tests pass
pnpm test:unit

# Both new test files present
ls src/components/mdx/code-filename.test.tsx src/components/mdx/inline-badge.test.tsx

# Both new files in vitest coverage config
grep -c 'code-filename.tsx' vitest.config.mts
grep -c 'inline-badge.tsx' vitest.config.mts

# Full coverage run passes with new files
pnpm test:coverage
```
</verification>

<success_criteria>
- `pnpm test:unit` exits 0 with both new test files contributing passing tests
- `code-filename.test.tsx` has tests for: filename text, FileIcon aria-hidden svg, aria-label format, children passthrough
- `inline-badge.test.tsx` has tests for: children, all 4 variants (primary/secondary/muted/destructive), span element type
- `vitest.config.mts` COMPONENT_FILES array contains both `'src/components/mdx/code-filename.tsx'` and `'src/components/mdx/inline-badge.tsx'`
- `pnpm test:coverage` exits 0 — both new files meet COMPONENT_TARGET (70/60/70/70) thresholds
</success_criteria>

<output>
After completion, create `.planning/phases/06-mdx-component-toolkit-expansion/06-02-SUMMARY.md`
</output>
