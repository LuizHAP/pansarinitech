---
phase: 06-mdx-component-toolkit-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/mdx/code-filename.tsx
  - src/components/mdx/inline-badge.tsx
  - src/components/mdx/index.ts
  - messages/en.json
  - messages/pt.json
autonomous: true
requirements:
  - MDX-01
  - MDX-02
  - MDX-03

must_haves:
  truths:
    - "A <CodeFilename filename='src/app/page.tsx'> wrapper in MDX renders a filename bar with FileIcon above the fenced code block"
    - "An <InlineBadge variant='destructive'>breaking change</InlineBadge> in MDX prose renders an inline rounded-full chip without breaking line height"
    - "Both components are importable from src/components/mdx/index.ts and callable from MDX bodies without any per-file import"
    - "Screen readers hear 'File: src/app/page.tsx' in EN and 'Arquivo: src/app/page.tsx' in PT from the CodeFilename wrapper"
    - "InlineBadge secondary variant uses bg-muted text-muted-foreground (not bg-secondary which is undefined in globals.css)"
  artifacts:
    - path: "src/components/mdx/code-filename.tsx"
      provides: "Async RSC — FileIcon + filename bar + children passthrough"
      exports: ["CodeFilename"]
    - path: "src/components/mdx/inline-badge.tsx"
      provides: "Sync RSC — 4-variant inline chip"
      exports: ["InlineBadge"]
    - path: "src/components/mdx/index.ts"
      provides: "Updated closed mdxComponents map"
      contains: "CodeFilename, InlineBadge"
    - path: "messages/en.json"
      provides: "EN aria-label prefix"
      contains: "codeFilename.ariaPrefix"
    - path: "messages/pt.json"
      provides: "PT aria-label prefix"
      contains: "codeFilename.ariaPrefix"
  key_links:
    - from: "MDX body"
      to: "src/components/mdx/index.ts"
      via: "mdxComponents map passed to compileMDX"
      pattern: "CodeFilename.*InlineBadge"
    - from: "src/components/mdx/code-filename.tsx"
      to: "messages/en.json + messages/pt.json"
      via: "getTranslations({ locale, namespace: 'mdx' })"
      pattern: "codeFilename\\.ariaPrefix"
---

<objective>
Build the two new MDX components (`CodeFilename` and `InlineBadge`), add the i18n aria-label key for CodeFilename to both message files, and register both components in the closed `mdxComponents` map so they are immediately available in all MDX bodies (blog posts and project case studies) without any per-file import.

Purpose: Phase 6's core deliverable — two new authoring primitives that enrich MDX content and unblock Phase 7's blog post authoring.
Output: `code-filename.tsx` (async RSC), `inline-badge.tsx` (sync RSC), updated `index.ts`, updated `messages/en.json`, updated `messages/pt.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-mdx-component-toolkit-expansion/06-CONTEXT.md
@.planning/phases/06-mdx-component-toolkit-expansion/06-RESEARCH.md
@.planning/phases/06-mdx-component-toolkit-expansion/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build CodeFilename async RSC with i18n and register it</name>
  <files>
    src/components/mdx/code-filename.tsx
    messages/en.json
    messages/pt.json
    src/components/mdx/index.ts
  </files>
  <read_first>
    - src/components/mdx/callout.tsx — the async RSC reference pattern to follow exactly (async function, getLocale + getTranslations, lucide icon, cn(), no "use client")
    - src/components/mdx/index.ts — the closed mdxComponents map to update
    - messages/en.json — the existing "mdx" namespace structure (callout, copyCode, toc keys) to append into
    - messages/pt.json — same, PT side
    - src/app/globals.css — confirm available token names: border-border, bg-muted/50, text-muted-foreground (do NOT use bg-secondary — undefined in this project)
  </read_first>
  <behavior>
    - CodeFilename({ filename: 'src/app/page.tsx', children: React.createElement('pre', null, 'code') }) renders a div with className containing 'rounded-md' and 'border-border' and 'overflow-hidden'
    - The filename bar div contains a FileIcon svg with aria-hidden="true" and a span with text 'src/app/page.tsx' and className containing 'font-mono' and 'text-muted-foreground'
    - The outer div has aria-label 'File: src/app/page.tsx' when locale is 'en'
    - Children are rendered inside the outer container (passthrough)
  </behavior>
  <action>
    Create src/components/mdx/code-filename.tsx as an async RSC following the callout.tsx pattern exactly. Key implementation details (per D-01 through D-05, UI-SPEC):

    - No "use client" directive — this is a server component
    - Import: FileIcon from 'lucide-react', getLocale + getTranslations from 'next-intl/server', cn from '@/lib/utils', ReactNode from 'react'
    - Props: { filename: string; children: ReactNode }
    - Call getLocale() then getTranslations({ locale, namespace: 'mdx' }) — same two-call pattern as callout.tsx
    - Outer container: `<div className="rounded-md overflow-hidden border border-border" aria-label={\`${t('codeFilename.ariaPrefix')}: ${filename}\`}>`
    - Filename bar: `<div className="flex items-center gap-2 border-b border-border bg-muted/50 px-4 py-2">`
    - FileIcon: className="size-3.5 shrink-0 text-muted-foreground" aria-hidden="true"
    - Filename span: className="text-xs font-mono text-muted-foreground"
    - Children rendered directly after the filename bar div (plain passthrough — no extra wrapper)

    Update messages/en.json: inside the existing "mdx" object, add a "codeFilename" key: `"codeFilename": { "ariaPrefix": "File" }`. Place it after the existing "toc" key. Do not remove or reorder existing keys.

    Update messages/pt.json: same structure, value "Arquivo": `"codeFilename": { "ariaPrefix": "Arquivo" }`.

    Update src/components/mdx/index.ts: add import for CodeFilename from './code-filename', add CodeFilename to the mdxComponents object (alongside Callout, Note, Warning, Stat, pre). Also add to the named exports line. Keep the existing closed-map comment.
  </action>
  <verify>
    <automated>pnpm test:unit --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|code-filename)"</automated>
  </verify>
  <done>
    - src/components/mdx/code-filename.tsx exists, exports async function CodeFilename
    - messages/en.json "mdx.codeFilename.ariaPrefix" equals "File"
    - messages/pt.json "mdx.codeFilename.ariaPrefix" equals "Arquivo"
    - src/components/mdx/index.ts imports CodeFilename and includes it in mdxComponents
    - TypeScript compiles without error: pnpm build exits 0 (or pnpm tsc --noEmit exits 0)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build InlineBadge sync RSC</name>
  <files>
    src/components/mdx/inline-badge.tsx
    src/components/mdx/index.ts
  </files>
  <read_first>
    - src/components/mdx/stat.tsx — the pure sync RSC reference pattern (no async, no next-intl, plain function export)
    - src/components/ui/badge.tsx — the Shadcn Badge file to confirm the destructive token pattern (bg-destructive/10 text-destructive) — observe the token names but DO NOT use shadcn Badge itself; InlineBadge is a custom span (D-06)
    - src/app/globals.css — confirm the OKLCH token values for primary, muted, muted-foreground, destructive, border
    - src/components/mdx/index.ts — current state after Task 1 completed it (to add InlineBadge alongside CodeFilename)
  </read_first>
  <behavior>
    - InlineBadge({ children: 'label' }) with no variant prop renders a span with className containing 'text-primary' and 'border-primary' (primary is default per D-08)
    - InlineBadge({ variant: 'secondary', children: 'label' }) renders a span with className containing 'bg-muted' and 'text-muted-foreground' — NOT 'bg-secondary' (--secondary is undefined in globals.css per RESEARCH Pitfall 3)
    - InlineBadge({ variant: 'muted', children: 'label' }) renders a span with className containing 'text-muted-foreground' and 'border-border'
    - InlineBadge({ variant: 'destructive', children: 'label' }) renders a span with className containing 'text-destructive' and 'border-destructive'
    - The span element is inline (not a div) and children are rendered as text content
  </behavior>
  <action>
    Create src/components/mdx/inline-badge.tsx as a pure sync RSC following the stat.tsx pattern. Key implementation details (per D-06, D-07, D-08, UI-SPEC):

    - No "use client", no async, no next-intl imports
    - Import: cn from '@/lib/utils', ReactNode from 'react'
    - Define type BadgeVariant = 'primary' | 'secondary' | 'muted' | 'destructive'
    - Define VARIANT_STYLES record keyed by BadgeVariant:
      - primary: 'border-primary/30 text-primary'
      - secondary: 'bg-muted text-muted-foreground' (NOT bg-secondary — token is undefined in this project's globals.css)
      - muted: 'border-border text-muted-foreground'
      - destructive: 'border-destructive/30 text-destructive'
    - Props: { variant?: BadgeVariant; children: ReactNode } — variant defaults to 'primary'
    - Return element: <span className={cn('inline-flex items-center rounded-full border px-2 py-0.5 text-xs font-medium', VARIANT_STYLES[variant])}>{children}</span>

    Update src/components/mdx/index.ts: add import for InlineBadge from './inline-badge', add InlineBadge to the mdxComponents object, add to named exports line. This file was partially updated in Task 1 — read it first to see current state before editing.
  </action>
  <verify>
    <automated>pnpm test:unit --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|inline-badge)"</automated>
  </verify>
  <done>
    - src/components/mdx/inline-badge.tsx exists, exports function InlineBadge
    - src/components/mdx/index.ts imports and exports InlineBadge, mdxComponents object contains InlineBadge key
    - No "bg-secondary" appears in inline-badge.tsx (grep -c 'bg-secondary' src/components/mdx/inline-badge.tsx equals 0)
    - TypeScript compiles without error
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX body → mdxComponents map | MDX author-supplied component names resolve against the closed map; unregistered names are ignored |
| filename prop → aria-label | String from MDX frontmatter/authoring rendered as JSX text — React escapes by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | mdxComponents map (index.ts) | mitigate | Keep closed-map pattern — only add CodeFilename and InlineBadge by name; do NOT spread external maps or use dynamic keys |
| T-06-02 | Tampering | CodeFilename filename prop | accept | filename is a string from MDX authoring (owner-controlled content); React JSX text rendering escapes HTML entities automatically — no dangerouslySetInnerHTML used |
| T-06-03 | Spoofing | InlineBadge variant prop | accept | variant is a typed union ('primary' | 'secondary' | 'muted' | 'destructive'); VARIANT_STYLES record covers all values; unknown variant falls back to primary |
</threat_model>

<verification>
After both tasks complete:

```bash
# TypeScript check
pnpm tsc --noEmit

# All existing tests still pass (no regressions)
pnpm test:unit

# CodeFilename exported from index
grep -c 'CodeFilename' src/components/mdx/index.ts

# InlineBadge exported from index
grep -c 'InlineBadge' src/components/mdx/index.ts

# bg-secondary not used in inline-badge.tsx (must be 0)
grep -v '^//' src/components/mdx/inline-badge.tsx | grep -c 'bg-secondary' || true

# i18n keys present
grep -c 'codeFilename' messages/en.json
grep -c 'codeFilename' messages/pt.json
```
</verification>

<success_criteria>
- `src/components/mdx/code-filename.tsx` exports `async function CodeFilename({ filename, children })` following the callout.tsx async RSC pattern
- `src/components/mdx/inline-badge.tsx` exports `function InlineBadge({ variant?, children })` with 4 variant styles, secondary uses `bg-muted text-muted-foreground`
- `messages/en.json` contains `"mdx.codeFilename.ariaPrefix": "File"` and `messages/pt.json` contains `"mdx.codeFilename.ariaPrefix": "Arquivo"`
- `src/components/mdx/index.ts` mdxComponents object contains both `CodeFilename` and `InlineBadge` keys (PascalCase)
- `pnpm test:unit` exits 0 (229+ tests passing, no regressions)
- `pnpm tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-mdx-component-toolkit-expansion/06-01-SUMMARY.md`
</output>
