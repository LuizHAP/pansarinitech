---
phase: 03-automated-content-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-post-prompt.md
  - content/blog/.pipeline-state.json
autonomous: true
requirements:
  - PIPE-02
  - PIPE-06
  - PIPE-07
must_haves:
  truths:
    - "content/blog/.pipeline-state.json exists and contains valid JSON with last_topic: null and the four-topic sequence"
    - "scripts/generate-post-prompt.md exists and contains all frontmatter constraints, voice instructions, and the bilingual generation instructions"
    - "The prompt file explicitly states the excerpt 40–280 char requirement and forbids inline JS"
  artifacts:
    - path: "content/blog/.pipeline-state.json"
      provides: "Initial topic rotation state — first run will pick nextjs-react-frontend (index 0 after null)"
      contains: "last_topic"
    - path: "scripts/generate-post-prompt.md"
      provides: "Generation prompt read by the workflow via $(cat scripts/generate-post-prompt.md)"
      contains: "excerpt MUST be 40"
  key_links:
    - from: ".github/workflows/generate-post.yml (Plan 03-02)"
      to: "scripts/generate-post-prompt.md"
      via: "STYLE_REF=$(cat content/blog/building-this-portfolio.en.mdx) and PROMPT=$(cat scripts/generate-post-prompt.md)"
      pattern: "cat scripts/generate-post-prompt.md"
    - from: ".github/workflows/generate-post.yml (Plan 03-02)"
      to: "content/blog/.pipeline-state.json"
      via: "node -e reads last_topic; agent updates it"
      pattern: "pipeline-state.json"
---

<objective>
Create the two static artifacts that the generation workflow (Plan 03-02) depends on: the initial topic-rotation state file and the external generation prompt.

Purpose: Separating the prompt into its own file makes it auditable, editable without YAML escaping, and reviewable in pull requests independent of workflow changes (per RESEARCH.md Open Question 3 recommendation). The state file bootstraps the topic rotation so the first workflow run deterministically picks `nextjs-react-frontend`.

Output: `scripts/generate-post-prompt.md` and `content/blog/.pipeline-state.json`
</objective>

<execution_context>
@/Users/luizpansarini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luizpansarini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-automated-content-pipeline/03-CONTEXT.md
@.planning/phases/03-automated-content-pipeline/03-RESEARCH.md
@.planning/phases/03-automated-content-pipeline/03-PATTERNS.md

<interfaces>
From src/lib/mdx/schema.ts — BlogFrontmatter Zod schema (authoritative shape the generated posts MUST satisfy):

  title: z.string().min(1)
  date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, 'date must be YYYY-MM-DD')
  excerpt: z.string().min(40).max(280)
  tags: z.array(z.string()).default([])
  draft: z.boolean().default(false)

From scripts/check-mdx-content.mjs:
  FILE_PATTERN = /^([a-z0-9-]+)\.(en|pt)\.mdx$/
  validatePostFrontmatter checks:
    - title: non-empty string
    - date: regex /^\d{4}-\d{2}-\d{2}$/ — 10 chars exactly, no time component
    - excerpt: 40–280 chars inclusive
    - tags: optional; if present must be string array
    - draft: optional; if present must be boolean

content/blog/building-this-portfolio.en.mdx voice traits:
  - H2 headings only (no H1 — page template renders title)
  - Direct, technical, low-fluff, first-person
  - Bold for key terms: **Next.js 16.2**
  - Code spans for tool names: `useActionState`
  - Fenced code blocks only for actual code — rare
  - Narrative paragraphs preferred over bullet lists
  - PT version is idiomatic Brazilian Portuguese, not a word-for-word translation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create initial .pipeline-state.json</name>
  <files>content/blog/.pipeline-state.json</files>
  <read_first>
    - .planning/phases/03-automated-content-pipeline/03-RESEARCH.md — Pattern 4 (.pipeline-state.json schema, lines 229–244) and Pattern 5 (field semantics)
    - .planning/phases/03-automated-content-pipeline/03-PATTERNS.md — Pipeline state section (no-analog file, schema from RESEARCH.md)
    - .planning/phases/03-automated-content-pipeline/03-CONTEXT.md — D-07 (last_topic field required), D-08 (committed to repo)
  </read_first>
  <action>
    Create content/blog/.pipeline-state.json with these exact field values (per D-07 and RESEARCH.md Pattern 4):
      last_topic: null (JSON null, not the string "null")
      run_count: 0
      last_run_date: null (JSON null)
      topic_sequence: ["nextjs-react-frontend", "software-engineering-career", "ai-in-development", "personal-projects-open-source"]

    The field `last_topic: null` causes the first workflow run to compute `next = sequence[0]` = "nextjs-react-frontend" (as specified in D-07 and RESEARCH.md Pattern 4 initial-state note).

    Do NOT add a `.gitignore` entry for this file — it is committed to the repo per D-08 so topic state is tracked across runs atomically with each PR.

    Note: `pnpm verify:posts` (check-mdx-content.mjs FILE_PATTERN `/^([a-z0-9-]+)\.(en|pt)\.mdx$/`) does not scan this dotfile — it will not trigger any CI validation for the state file itself.
  </action>
  <verify>
    <automated>node -e "const s = JSON.parse(require('fs').readFileSync('content/blog/.pipeline-state.json','utf8')); console.assert(s.last_topic === null); console.assert(s.run_count === 0); console.assert(Array.isArray(s.topic_sequence) && s.topic_sequence.length === 4); console.log('state file valid')"</automated>
  </verify>
  <done>
    content/blog/.pipeline-state.json exists with last_topic=null, run_count=0, last_run_date=null, and topic_sequence containing all four topic slugs in the canonical order.
  </done>
  <acceptance_criteria>
    - File content parses as valid JSON (no trailing commas, no comments)
    - last_topic is JSON null (not the string "null" or empty string)
    - run_count is integer 0
    - last_run_date is JSON null
    - topic_sequence is an array of exactly four strings: "nextjs-react-frontend", "software-engineering-career", "ai-in-development", "personal-projects-open-source" in that order
    - The `node -e` verify command exits 0 with output "state file valid"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create scripts/generate-post-prompt.md</name>
  <files>scripts/generate-post-prompt.md</files>
  <read_first>
    - content/blog/building-this-portfolio.en.mdx — Full file; this is the style reference the prompt embeds inline
    - .planning/phases/03-automated-content-pipeline/03-RESEARCH.md — Pattern 5 (frontmatter shape), Pattern 7 (MDX body constraints), Pitfalls 1–7 (each drives an explicit constraint in the prompt), Open Question 3 (external prompt file rationale)
    - .planning/phases/03-automated-content-pipeline/03-PATTERNS.md — Pattern assignments, MDX frontmatter pattern for generated posts, Voice and structure pattern section
    - .planning/phases/03-automated-content-pipeline/03-CONTEXT.md — D-04 (style reference), D-05 (800–1200 words), D-06 (single invocation writes both files), D-09 (draft: true)
    - src/lib/mdx/schema.ts — Exact field names and types (title, date, excerpt, tags, draft)
  </read_first>
  <action>
    Create scripts/generate-post-prompt.md. The workflow will inject this via:
      PROMPT_TEMPLATE=$(cat scripts/generate-post-prompt.md)
    and then substitute $SLUG, $TOPIC, $TODAY, and $STYLE_REF before passing to claude -p.

    The prompt must contain these sections in this order:

    1. ROLE — "You are writing a bilingual technical blog post for Luiz Pansarini's portfolio at pansarini.tech."

    2. TASK — Instructs the agent to write TWO files using Write tool:
       - content/blog/${SLUG}.en.mdx (English)
       - content/blog/${SLUG}.pt.mdx (Brazilian Portuguese — not a translation, write naturally)
       And to update content/blog/.pipeline-state.json with:
         last_topic: "${TOPIC}", run_count incremented by 1 from existing, last_run_date: "${TODAY}", topic_sequence unchanged.

    3. TOPIC — "Topic area: ${TOPIC}. Write substantive content relevant to this area."

    4. FRONTMATTER — Must include the exact required shape. Critical: use ${TODAY} for date (env var injected by workflow). State excerpt constraint as: "The excerpt field MUST be between 40 and 280 characters inclusive. Count the characters explicitly before writing. Do not write fewer than 40 characters. A minimum viable excerpt: 'A deep dive into ${TOPIC} from a Principal Engineer building at BR scale.' (that is 73 characters — aim for similar or longer)." Reproduce the full frontmatter block:
         ---
         title: "<title in the post's language>"
         date: "${TODAY}"
         excerpt: "<YOUR EXCERPT: count characters, must be 40–280>"
         tags:
           - <tag1>
           - <tag2>
         draft: true
         ---

    5. CONTENT RULES — Explicit list:
       - Length: 800–1200 words per locale (count approximately)
       - H2 headings only — the page template renders the title as H1; do not write an H1
       - Voice: technical, direct, low-fluff, first-person past tense for decisions
       - Bold for key terms inline using **term**
       - Code spans using `backticks` for tool names, file paths, config keys, package names
       - Fenced code blocks using triple backticks only for actual code — rare, only when truly illustrative
       - Narrative paragraphs preferred over bullet lists
       - FORBIDDEN — no inline JavaScript expressions: {value}, {expression}, no import statements, no export statements, no JSX syntax in the body
       - FORBIDDEN — the date field value must be exactly "${TODAY}" (a 10-character YYYY-MM-DD string); do not write "May 13, 2026" or any other format
       - The PT version must read naturally in idiomatic Brazilian Portuguese — rethink phrasing rather than translating word-for-word
       - Slug in filenames must be exactly: ${SLUG} (already lowercase alphanumeric with hyphens — do not change it)

    6. STYLE REFERENCE — "Match this voice and structure (existing post for reference):" followed by:
       ${STYLE_REF}

    The prompt file itself should contain literal ${SLUG}, ${TODAY}, ${TOPIC}, ${STYLE_REF} placeholders. The workflow injects actual values before passing to claude -p.

    Format the file as Markdown (the .md extension) so it is readable in GitHub's UI. Use standard Markdown headers for each section.
  </action>
  <verify>
    <automated>grep -c "40 and 280 characters" scripts/generate-post-prompt.md && grep -c "draft: true" scripts/generate-post-prompt.md && grep -c "FORBIDDEN" scripts/generate-post-prompt.md && grep -c "\${TODAY}" scripts/generate-post-prompt.md && grep -c "\${SLUG}" scripts/generate-post-prompt.md</automated>
  </verify>
  <done>
    scripts/generate-post-prompt.md exists and contains:
    - The excerpt 40–280 char constraint with explicit instruction to count
    - draft: true in the required frontmatter block
    - At least one FORBIDDEN block covering inline JS and date format
    - The ${TODAY} and ${SLUG} placeholders for workflow injection
    - The ${STYLE_REF} placeholder
  </done>
  <acceptance_criteria>
    - grep -c "40 and 280 characters" returns at least 1 (excerpt constraint present)
    - grep -c "draft: true" returns at least 1 (draft flag in frontmatter template)
    - grep -c "FORBIDDEN" returns at least 1 (inline JS prohibition clearly labeled)
    - grep -c "\${TODAY}" returns at least 1 (date injection placeholder present)
    - grep -c "\${SLUG}" returns at least 2 (slug referenced in both the filenames section and content rules)
    - grep -c "\${STYLE_REF}" returns at least 1 (style reference placeholder present)
    - grep -c "\${TOPIC}" returns at least 2 (topic referenced in task and content rules sections)
    - File is valid Markdown — no YAML frontmatter needed (it is a prompt template, not an MDX post)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → reader | content/blog/.pipeline-state.json is committed to the repo and readable by anyone with repo access |
| prompt → claude agent | scripts/generate-post-prompt.md is read by the workflow and passed to claude -p; it controls what the agent writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | content/blog/.pipeline-state.json | accept | Committed to repo; any change is visible in git history; state corruption triggers wrong topic selection at worst — no security impact, correctable by editing JSON |
| T-03-02 | Tampering | scripts/generate-post-prompt.md | accept | Prompt is static config committed to repo; changes visible in git diff on every PR; no secrets in the file; worst case = bad prompt = bad post that fails verify:posts |
| T-03-03 | Information Disclosure | scripts/generate-post-prompt.md | accept | File contains no secrets; ANTHROPIC_API_KEY is injected by workflow secrets, not by the prompt file; style reference content is already public blog post |
</threat_model>

<verification>
- node -e "JSON.parse(require('fs').readFileSync('content/blog/.pipeline-state.json','utf8'))" exits 0
- grep -c "40 and 280 characters" scripts/generate-post-prompt.md returns ≥ 1
- grep -c "FORBIDDEN" scripts/generate-post-prompt.md returns ≥ 1
- grep -c '"\${TODAY}"' scripts/generate-post-prompt.md returns ≥ 1
- Both files visible in git status (untracked → staged)
</verification>

<success_criteria>
- content/blog/.pipeline-state.json is valid JSON with last_topic=null and all four topics in topic_sequence
- scripts/generate-post-prompt.md explicitly states the excerpt 40–280 char constraint, forbids inline JS, and contains ${TODAY}, ${SLUG}, ${TOPIC}, ${STYLE_REF} injection placeholders
- pnpm verify:posts passes (state file is a dotfile, skipped by FILE_PATTERN — no interference with existing blog posts)
</success_criteria>

<output>
After completion, create .planning/phases/03-automated-content-pipeline/03-01-SUMMARY.md
</output>
