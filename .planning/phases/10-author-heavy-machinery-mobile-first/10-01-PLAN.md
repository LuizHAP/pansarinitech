---
phase: 10-author-heavy-machinery-mobile-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/projects/machinery-mobile-first/images/hero.jpg
  - content/projects/machinery-mobile-first.en.mdx
  - content/projects/machinery-mobile-first.pt.mdx
  - src/components/sections/case-study-hero.tsx
  - src/app/[locale]/projects/page.tsx
  - src/components/sections/featured-projects-teaser.tsx
  - messages/en.json
  - messages/pt.json
autonomous: true
requirements:
  - CASE-15
  - CASE-16
  - CASE-17
  - CASE-18
  - CASE-19
  - CASE-20
user_setup: []

must_haves:
  truths:
    - "Visitor opens /projects/machinery-mobile-first in EN locale (NEXT_LOCALE=en cookie) and reads full case study with Problem / Solution / Impact / Stack sections"
    - "Visitor opens /projects/machinery-mobile-first in PT locale (NEXT_LOCALE=pt cookie) and reads same sections fully translated to Brazilian Portuguese"
    - "Visitor browsing /projects sees five case study cards including Heavy Machinery mobile-first with hero image rendered (no broken image)"
    - "Page <title>, meta description, and og:image tags reflect the mobile-first title and blurb for the active locale (no fallbacks, no empty strings)"
    - "JSON-LD script tag on every project page (all 5 case studies) declares @type 'Article' with headline field"
    - "MDX body of both mobile-first files contains at least one of <Callout>, <CodeFilename>, or <InlineBadge> in a meaningful (non-decorative) position"
    - "i18n strings for /projects listing description and home SEO description say 'Five case studies' / 'Cinco estudos de caso' (not 'Four' / 'Quatro')"
    - "next build succeeds; pnpm verify:projects passes; bilingual parity check does not throw"
  artifacts:
    - path: "content/projects/machinery-mobile-first/images/hero.jpg"
      provides: "Hero image (JPEG 1024x640, 16:10, saber-blue gradient placeholder)"
      contains: "JPEG binary"
    - path: "content/projects/machinery-mobile-first.en.mdx"
      provides: "English mobile-first case study MDX source"
      contains: "title: \"Heavy Machinery mobile-first\""
      min_lines: 60
    - path: "content/projects/machinery-mobile-first.pt.mdx"
      provides: "Portuguese mobile-first case study MDX source"
      contains: "title: \"Mobile-first de máquinas pesadas\""
      min_lines: 60
    - path: "src/components/sections/case-study-hero.tsx"
      provides: "HERO_IMAGES map entry for 'machinery-mobile-first'"
      contains: "'machinery-mobile-first': machineryMobileFirst"
    - path: "src/app/[locale]/projects/page.tsx"
      provides: "HERO_IMAGES map entry for 'machinery-mobile-first' (listing)"
      contains: "'machinery-mobile-first': machineryMobileFirst"
    - path: "src/components/sections/featured-projects-teaser.tsx"
      provides: "HERO_IMAGES map entry for 'machinery-mobile-first' (featured teaser)"
      contains: "'machinery-mobile-first': machineryMobileFirst"
    - path: "messages/en.json"
      provides: "EN i18n strings updated to 'Five case studies'"
      contains: "Five case studies"
    - path: "messages/pt.json"
      provides: "PT i18n strings updated to 'Cinco estudos de caso'"
      contains: "Cinco estudos de caso"
  key_links:
    - from: "src/components/sections/case-study-hero.tsx"
      to: "content/projects/machinery-mobile-first/images/hero.jpg"
      via: "static import"
      pattern: "import machineryMobileFirst from '\\.\\./\\.\\./\\.\\./content/projects/machinery-mobile-first/images/hero\\.jpg'"
    - from: "src/app/[locale]/projects/page.tsx"
      to: "content/projects/machinery-mobile-first/images/hero.jpg"
      via: "static import (4 levels deep)"
      pattern: "import machineryMobileFirst from '\\.\\./\\.\\./\\.\\./\\.\\./content/projects/machinery-mobile-first/images/hero\\.jpg'"
    - from: "src/components/sections/featured-projects-teaser.tsx"
      to: "content/projects/machinery-mobile-first/images/hero.jpg"
      via: "static import (3 levels deep)"
      pattern: "import machineryMobileFirst from '\\.\\./\\.\\./\\.\\./content/projects/machinery-mobile-first/images/hero\\.jpg'"
    - from: "content/projects/machinery-mobile-first.en.mdx"
      to: "src/lib/mdx/schema.ts ProjectFrontmatter"
      via: "Zod-validated frontmatter at build"
      pattern: "title:.*role:.*year:.*stack:.*blurb:.*heroImage:.*tags:.*featured:.*order:"
---

<objective>
Author the bilingual Heavy Machinery Mobile-First case study and integrate it atomically into the existing MDX pipeline. The case study must render at /projects/machinery-mobile-first in both locales, appear in the /projects listing, expose correct OG/JSON-LD metadata as Article schema, and the build must succeed with bilingual parity intact.

Purpose: Closes 6 requirements (CASE-15 through CASE-20) in a single atomic content+integration unit. The build-time guards in HERO_IMAGES and verifyParity() mean every file in this plan must land together; splitting across plans would break next build between commits.

Output:
- New hero image asset and bilingual MDX content files
- Three HERO_IMAGES map registrations (extending existing maps in case-study-hero.tsx, projects/page.tsx, featured-projects-teaser.tsx)
- i18n string updates "Four" -> "Five" in 4 locations (2 keys x 2 locales)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-author-heavy-machinery-mobile-first/10-RESEARCH.md
@.planning/phases/10-author-heavy-machinery-mobile-first/10-PATTERNS.md

<!-- Existing case study analog files (read these to mirror frontmatter shape, body structure, and PT translation register) -->
@content/projects/machinery-partner-ecommerce.en.mdx
@content/projects/machinery-partner-ecommerce.pt.mdx
@content/projects/LICENSE-images.txt

<!-- Files being modified -->
@src/components/sections/case-study-hero.tsx
@src/app/[locale]/projects/page.tsx
@src/components/sections/featured-projects-teaser.tsx
@messages/en.json
@messages/pt.json

<!-- Reference for Article JSON-LD shape (already applied to project pages since Phase 8) -->
@src/app/[locale]/blog/[slug]/page.tsx

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/lib/mdx/schema.ts (ProjectFrontmatter Zod schema, REQUIRED fields):
- title: string, min 1 char
- role: string, min 1 char
- year: number int, range 2018..2030
- stack: string[], min 1 item
- blurb: string, min 20 chars, max 280 chars (HARD constraint — Zod throws if outside range; PT runs ~15-20% longer than EN)
- heroImage: string, min 1 char (the "./slug/images/hero.jpg" frontmatter string — NOT used for image loading at runtime)
- tags: string[], default []
- featured: boolean, default true
- order: number int >= 1, default 99

From src/components/mdx/index.ts (closed mdxComponents map — only these JSX tags work in MDX bodies):
- Callout (props: type?: 'info'|'warn'|'error', label?: string)
- Note (alias: <Callout type="info">)
- Warning (alias: <Callout type="warn">)
- CodeFilename (props: filename: string) — wraps a fenced code block
- InlineBadge (props: variant: 'primary'|'secondary'|'muted'|'destructive')
- Stat (props: number: string, label: string)

From src/components/sections/case-study-hero.tsx lines 17–23 (current HERO_IMAGES map — add 5th entry):
- Record<string, StaticImageData> keyed by slug
- Build throws at line 33 if slug missing: "[case-study-hero] No hero image registered for slug ..."

From src/app/[locale]/projects/page.tsx lines 32–36 (current HERO_IMAGES map — `as const`, add 5th entry):
- Type-narrowed via `as const`; new slug must be in map or TS errors on line 62 `HERO_IMAGES[p.slug as keyof typeof HERO_IMAGES]`

From src/components/sections/featured-projects-teaser.tsx lines 22–26 (current HERO_IMAGES map — `as const`, add 5th entry):
- Type-narrowed via `as const`; new slug must be in map or TS errors on line 73

From src/i18n/routing.ts line 6: localePrefix: 'never'
- URLs are /projects/machinery-mobile-first for BOTH locales (locale via NEXT_LOCALE cookie, NOT URL prefix)
- ROADMAP wording "/en/projects/..." and "/pt/projetos/..." does NOT exist; render at /projects/machinery-mobile-first only

From src/app/[locale]/projects/[slug]/page.tsx lines 52–69:
- JSON-LD already emits @type: 'Article' (changed in Phase 8). No JSON-LD changes needed for Phase 10.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hero image asset and directory</name>
  <files>content/projects/machinery-mobile-first/images/hero.jpg</files>
  <read_first>
    - content/projects/LICENSE-images.txt (hero image conventions, palette guidance, NDA T-03-03 constraint)
    - content/projects/machinery-partner-ecommerce/images/hero.jpg (reference dimensions/size: 1024x640, ~15-18KB JPEG)
    - .planning/phases/10-author-heavy-machinery-mobile-first/10-PATTERNS.md (Hero Image Conventions table)
    - .planning/phases/10-author-heavy-machinery-mobile-first/10-RESEARCH.md (Hero Image Conventions section)
  </read_first>
  <action>
    Create directory content/projects/machinery-mobile-first/images/ then produce a 1024x640 progressive JPEG named hero.jpg inside it. Image must be abstract (NO employer screenshots, NO Machinery Partner brand assets) per LICENSE-images.txt NDA mitigation T-03-03. Use a saber-blue / Jedi-leaning gradient or geometric abstraction so it reads correctly in light mode (consistent with the existing hero images). Use the `sharp` Node package or ImageMagick (`convert`) to generate; an abstract approach: write a small inline Node script using `sharp` that creates a linear-gradient JPEG (oklch saber-blue stops) at 1024x640 with quality ~80 to target ~15-18KB output. After generation, verify dimensions via `file content/projects/machinery-mobile-first/images/hero.jpg` (expect "JPEG ... 1024x640"). Do NOT exceed 50KB; do NOT use PNG (the existing convention is JPEG and Next.js static-import generates blurDataURL from JPEG just fine).
  </action>
  <verify>
    <automated>file content/projects/machinery-mobile-first/images/hero.jpg | grep -E "JPEG.*1024x640" && test "$(stat -f%z content/projects/machinery-mobile-first/images/hero.jpg 2>/dev/null || stat -c%s content/projects/machinery-mobile-first/images/hero.jpg)" -lt 51200</automated>
  </verify>
  <acceptance_criteria>
    - File content/projects/machinery-mobile-first/images/hero.jpg exists
    - `file` reports format "JPEG" and dimensions "1024x640"
    - File size < 50KB (target 15-18KB)
    - Image is abstract gradient/geometric (no recognisable logos, screenshots, or brand assets)
    - Color palette leans saber-blue (Jedi side) consistent with content/projects/LICENSE-images.txt directive
  </acceptance_criteria>
  <done>
    Hero image asset exists at the correct path with correct format, dimensions, and size; ready to be statically imported by case-study-hero.tsx, projects/page.tsx, and featured-projects-teaser.tsx in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author bilingual mobile-first case study MDX files</name>
  <files>content/projects/machinery-mobile-first.en.mdx, content/projects/machinery-mobile-first.pt.mdx</files>
  <read_first>
    - content/projects/machinery-partner-ecommerce.en.mdx (canonical EN frontmatter shape and section structure)
    - content/projects/machinery-partner-ecommerce.pt.mdx (canonical PT frontmatter shape and section structure; PT register/tone)
    - src/lib/mdx/schema.ts (ProjectFrontmatter Zod schema — confirm exact field types and constraints)
    - src/components/mdx/index.ts (mdxComponents closed map — only these JSX tags work in MDX bodies)
    - .planning/phases/10-author-heavy-machinery-mobile-first/10-PATTERNS.md (frontmatter substitution targets, body structure template)
    - .planning/phases/10-author-heavy-machinery-mobile-first/10-RESEARCH.md (open questions: year=2024 per planning context, blurb length constraint)
  </read_first>
  <action>
    Create content/projects/machinery-mobile-first.en.mdx and content/projects/machinery-mobile-first.pt.mdx with the following frontmatter values (both files MUST be committed together — `verifyParity()` throws at build if either is missing).

    Frontmatter — EN file (title and blurb are locale-specific; all other fields are identical across both files per bilingual parity rule):
    - title: "Heavy Machinery mobile-first"
    - role: "Principal Software Engineer"
    - year: 2024 (per RESEARCH.md Assumptions Log A1 — same project as the ecommerce case study; add a top-of-file MDX HTML comment `<!-- TODO(Luiz): confirm mobile-first case study year -->` so it surfaces in `git diff` for confirmation)
    - stack array: React Native, Expo, TypeScript, Fastify, Bun, Odoo
    - blurb (EN, must be 20-280 chars): "Native mobile app for US heavy machinery buyers — React Native + Expo, offline cart-state contract, and App Store review."
    - heroImage: "./machinery-mobile-first/images/hero.jpg"
    - tags: mobile, react-native, e-commerce
    - featured: true
    - order: 5

    Frontmatter — PT file (locale-specific title + blurb; everything else identical to EN):
    - title: "Mobile-first de máquinas pesadas"
    - blurb (PT, must be 20-280 chars): "App mobile nativo para compradores de máquinas pesadas nos EUA — React Native + Expo, contrato de carrinho offline e revisão da App Store."
    - All other fields identical to EN file

    MDX body structure (both files; PT uses translated headings "Problema / Solução / Impacto / Stack"; EN uses "Problem / Solution / Impact / Stack"):
    1. ## Problem (EN) / ## Problema (PT) — 2 paragraphs covering: heavy machinery buyers need to browse, configure, and order equipment from mobile devices on-site at job sites; field workers don't have laptops, connectivity at construction sites is unreliable, the web app was too slow on mobile.
    2. ## Solution (EN) / ## Solução (PT) — 3-4 paragraphs covering React Native + Expo as the foundation, offline-first cart-state, LTE fallback, Expo workflow (OTA updates, EAS builds, App Store review process). MUST include at least ONE MDX toolkit component (CASE-18) — recommended: a `<Callout type="info">` highlighting the cart-state-first architectural decision AND a `<CodeFilename filename="mobile/cart-state.ts">` block wrapping a short TypeScript fenced code block showing the cart state shape (use the example from PATTERNS.md). At least one toolkit component per file is REQUIRED.
    3. ## Impact (EN) / ## Impacto (PT) — narrative paragraph + at least one `<Stat>` line for the headline outcome (e.g. "98% cart operations available offline before network sync" or "4.8★ App Store rating after first release").
    4. ## Stack — bulleted list with bold sub-headers (Mobile / Gateway / Cart-state), mirroring the structure of machinery-partner-ecommerce.en.mdx.

    Hard MDX constraints (do not violate — silent strip or build throw):
    - NO `{expression}` JS interpolation in MDX bodies (blocked by next-mdx-remote@^6 `blockJS: true` default)
    - NO `import` / `export` statements inside MDX body (blocked by `blockDangerousJS: true` default)
    - ONLY use JSX tags from the closed mdxComponents map: Callout, Note, Warning, CodeFilename, InlineBadge, Stat
    - Standard markdown headings (##), paragraphs, bold/italic, fenced code blocks, and bulleted lists are all fine

    Validation step: after authoring both files, run `pnpm verify:projects` — it executes scripts/check-mdx-content.mjs which validates frontmatter against the Zod schema AND enforces bilingual parity. Both files together must pass.
  </action>
  <verify>
    <automated>pnpm verify:projects 2>&1 | tee /tmp/verify-projects.log && grep -E "ok|passed|success" /tmp/verify-projects.log && grep -c '<Callout\|<CodeFilename\|<InlineBadge' content/projects/machinery-mobile-first.en.mdx | awk '{ if ($1 < 1) exit 1 }' && grep -c '<Callout\|<CodeFilename\|<InlineBadge' content/projects/machinery-mobile-first.pt.mdx | awk '{ if ($1 < 1) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist: content/projects/machinery-mobile-first.en.mdx and content/projects/machinery-mobile-first.pt.mdx
    - `pnpm verify:projects` exits 0 (covers Zod frontmatter validation + verifyParity bilingual check)
    - EN file frontmatter line `title: "Heavy Machinery mobile-first"` present
    - PT file frontmatter line `title: "Mobile-first de máquinas pesadas"` present
    - Both files declare `year: 2024`, `order: 5`, `featured: true`, `heroImage: "./machinery-mobile-first/images/hero.jpg"`
    - Both files declare `stack:` containing React Native, Expo, TypeScript, Fastify, Bun, Odoo
    - Both files declare `tags:` containing mobile, react-native, e-commerce
    - EN blurb length between 20 and 280 chars (Zod-enforced)
    - PT blurb length between 20 and 280 chars (Zod-enforced)
    - EN file body contains all four headings: "## Problem", "## Solution", "## Impact", "## Stack"
    - PT file body contains all four headings: "## Problema", "## Solução", "## Impacto", "## Stack"
    - Each file body contains at least one `<Callout`, `<CodeFilename`, or `<InlineBadge` JSX tag (CASE-18)
    - Each file body contains at least one `<Stat ` JSX tag highlighting an outcome metric
    - EN file body text mentions "React Native" / "Expo" / "App Store" / "cart-state" (Problem/Solution/Results structure with mobile-first narrative — CASE-16)
    - PT file body fully localized (no untranslated EN phrases except technical proper nouns like "Expo", "React Native", "App Store", "Fastify")
    - No `{...}` JS expression interpolation anywhere in either MDX body
    - No `import` or `export` statements anywhere in either MDX body
    - HTML comment `<!-- TODO(Luiz): confirm mobile-first case study year -->` present in at least one of the files (year-confirmation flag per RESEARCH.md A1)
  </acceptance_criteria>
  <done>
    Both .en.mdx and .pt.mdx files exist with valid Zod-passing frontmatter, full Problem/Solution/Impact/Stack structure in both locales, at least one MDX toolkit component per file, and `pnpm verify:projects` exits 0. Ready for HERO_IMAGES wiring in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire integration (HERO_IMAGES x3, i18n strings)</name>
  <files>src/components/sections/case-study-hero.tsx, src/app/[locale]/projects/page.tsx, src/components/sections/featured-projects-teaser.tsx, messages/en.json, messages/pt.json</files>
  <read_first>
    - src/components/sections/case-study-hero.tsx (current HERO_IMAGES map lines 17-23 + build-time throw at line 33)
    - src/app/[locale]/projects/page.tsx (current HERO_IMAGES map lines 32-36 with `as const`)
    - src/components/sections/featured-projects-teaser.tsx (current HERO_IMAGES map lines 22-26 with `as const`)
    - messages/en.json (lines 78 and 169 — both "Four case studies" string locations)
    - messages/pt.json (lines 78 and 169 — both "Quatro estudos de caso" string locations)
    - .planning/phases/10-author-heavy-machinery-mobile-first/10-PATTERNS.md (Pattern Assignments sections for each file)
  </read_first>
  <action>
    Apply five coordinated edits that wire the new case study into the codebase. ALL edits must land in the same commit — partial wiring causes next build to throw via the case-study-hero.tsx slug guard or TypeScript `as const` keyof narrowing.

    Edit A — src/components/sections/case-study-hero.tsx:
    - Add a new static import after the existing uauboxHero import (line 15): `import machineryMobileFirst from '../../../content/projects/machinery-mobile-first/images/hero.jpg';` (3 levels up — depth matches existing 4 imports).
    - Extend the HERO_IMAGES Record (lines 17-23) by appending one entry: `'machinery-mobile-first': machineryMobileFirst,` after the uaubox-design-system entry.
    - Do NOT modify the build-time guard at line 33 (it remains the safety net for future case studies).

    Edit B — src/app/[locale]/projects/page.tsx:
    - Add a new static import after the existing uauboxHero import (line 30): `import machineryMobileFirst from '../../../../content/projects/machinery-mobile-first/images/hero.jpg';` (4 levels up — note the deeper relative path due to file location).
    - Extend the HERO_IMAGES `as const` object literal (lines 32-36) by appending one entry: `'machinery-mobile-first': machineryMobileFirst,` after the uaubox-design-system entry. The `as const` widens the keyof union automatically; no other type changes needed.
    - Verify TypeScript still compiles by inspecting line 62: `HERO_IMAGES[p.slug as keyof typeof HERO_IMAGES]` — no edit, but confirm the new slug is now part of the `keyof` union.

    Edit C — src/components/sections/featured-projects-teaser.tsx:
    - Add a new static import after the existing magaluSuperapp import (line 20): `import machineryMobileFirst from '../../../content/projects/machinery-mobile-first/images/hero.jpg';` (3 levels up).
    - Extend the HERO_IMAGES `as const` object literal (lines 22-26) by appending one entry: `'machinery-mobile-first': machineryMobileFirst,` after the magaluSuperapp entry.
    - Note: the featured teaser shows only the first 3 featured projects (`.slice(0, 3)`). With `order: 5`, this case study will NOT appear on the home page teaser, but WILL appear on the full `/projects` listing page.

    Edit D — messages/en.json:
    - Line 78 `projects.listingDescription`: change `"Four case studies: heavy machinery e-commerce, no-code → Next.js migration, React Native at scale, and a design system built from scratch."` → `"Five case studies: heavy machinery e-commerce, no-code → Next.js migration, React Native at scale, a design system built from scratch, and a mobile-first native app."`
    - Line 169 `seo.descriptions.projects`: apply the same replacement string (both keys receive the same new content; they serve different consumers — listingDescription feeds /projects generateMetadata, seo.descriptions.projects feeds home-page SEO).

    Edit E — messages/pt.json:
    - Line 78 `projects.listingDescription`: change `"Quatro estudos de caso: e-commerce de máquinas pesadas, migração no-code → Next.js, React Native em escala e um design system construído do zero."` → `"Cinco estudos de caso: e-commerce de máquinas pesadas, migração no-code → Next.js, React Native em escala, um design system construído do zero e um app mobile-first nativo."`
    - Line 169 `seo.descriptions.projects`: apply the same PT replacement string.

    After all five edits, run `pnpm lint` (biome check) to confirm no JSON syntax errors, then `pnpm build` to confirm the case-study-hero guard does not throw and TypeScript narrowing accepts the new slug.
  </action>
  <verify>
    <automated>grep -c "'machinery-mobile-first': machineryMobileFirst" src/components/sections/case-study-hero.tsx src/app/\[locale\]/projects/page.tsx src/components/sections/featured-projects-teaser.tsx | grep -v ":0$" | wc -l | awk '{ if ($1 != 3) exit 1 }' && grep -c "Five case studies" messages/en.json | awk '{ if ($1 < 2) exit 1 }' && grep -c "Four case studies" messages/en.json | awk '{ if ($1 > 0) exit 1 }' && grep -c "Cinco estudos de caso" messages/pt.json | awk '{ if ($1 < 2) exit 1 }' && grep -c "Quatro estudos de caso" messages/pt.json | awk '{ if ($1 > 0) exit 1 }' && pnpm build 2>&1 | tail -20 | grep -E "Compiled successfully|Generating static pages"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/case-study-hero.tsx contains the literal string `import machineryMobileFirst from '../../../content/projects/machinery-mobile-first/images/hero.jpg'`
    - src/components/sections/case-study-hero.tsx HERO_IMAGES map contains the literal entry `'machinery-mobile-first': machineryMobileFirst`
    - src/app/[locale]/projects/page.tsx contains the literal string `import machineryMobileFirst from '../../../../content/projects/machinery-mobile-first/images/hero.jpg'`
    - src/app/[locale]/projects/page.tsx HERO_IMAGES `as const` object contains the literal entry `'machinery-mobile-first': machineryMobileFirst`
    - src/components/sections/featured-projects-teaser.tsx contains the literal string `import machineryMobileFirst from '../../../content/projects/machinery-mobile-first/images/hero.jpg'`
    - src/components/sections/featured-projects-teaser.tsx HERO_IMAGES `as const` object contains the literal entry `'machinery-mobile-first': machineryMobileFirst`
    - messages/en.json contains "Five case studies" appearing in at least 2 distinct lines (listingDescription + seo.descriptions.projects)
    - messages/en.json contains zero occurrences of "Four case studies" (legacy string fully replaced)
    - messages/pt.json contains "Cinco estudos de caso" appearing in at least 2 distinct lines
    - messages/pt.json contains zero occurrences of "Quatro estudos de caso" (legacy string fully replaced)
    - `pnpm build` exits 0 (next build succeeds; case-study-hero throw guard does not fire; TypeScript `as const` keyof narrowing accepts new slug)
    - `pnpm verify:projects` exits 0 (bilingual parity + Zod validation across all five case studies)
    - `pnpm lint` exits 0 (biome JSON + TS lint clean)
  </acceptance_criteria>
  <done>
    All five integration edits applied atomically. Build succeeds, case-study renders at /projects/machinery-mobile-first in both locales, listing page shows five cards, JSON-LD declares Article schema globally, i18n strings reflect five case studies in both locales. All six requirements (CASE-15 through CASE-20) satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → MDX body | MDX content is authored by the project owner (Luiz) — owner-only content, not user input. Compiled at build time by next-mdx-remote@^6 in RSC. |
| Build → MDX compiler | `blockJS: true` + `blockDangerousJS: true` defaults (next-mdx-remote v6) strip `{expr}` JS expressions and `import`/`export` from MDX bodies — defence in depth even though content is owner-authored. |
| Frontmatter string → JSON-LD url | The `project.heroImage` frontmatter value is interpolated into the JSON-LD `image` field via `${SITE_URL}/${project.heroImage.replace(/^\.?\//, '')}`. Owner-controlled, but flagged for completeness. |
| Static image path → Next.js bundler | `hero.jpg` is resolved via module-level static import (compile-time path resolution); no runtime path traversal vector. |
| `<JsonLd dangerouslySetInnerHTML>` → DOM | json-ld.tsx serialises a Zod-shaped object with `<` → `&lt;` escaping (verified in v1.2 Phase 4 SUMMARY). Owner-only schema input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering / XSS | Mobile-first MDX body (`machinery-mobile-first.{en,pt}.mdx`) | mitigate | next-mdx-remote@^6 `blockJS: true` + `blockDangerousJS: true` defaults (locked in v1.3 D-17) strip `{expr}` JS and import/export from MDX. Author writes pure prose + closed-map JSX tags only — enforced in Task 2 acceptance criteria (no `{...}` interpolation, no import/export). |
| T-10-02 | Tampering / Path traversal | Hero image static import path in case-study-hero.tsx, projects/page.tsx, and featured-projects-teaser.tsx | mitigate | Path is a module-level static import literal — resolved at compile time by Next.js bundler. Runtime path injection impossible. Frontmatter `heroImage` string is reference-only (NOT used for loading). |
| T-10-03 | Tampering / XSS via JSON-LD | `<JsonLd dangerouslySetInnerHTML>` in projects/[slug]/page.tsx (Article schema) | mitigate | JsonLd component (v1.2 Phase 4) HTML-escapes `<` → `&lt;` inside the JSON.stringify output. Schema fields (title, blurb, stack, year) are all Zod-validated owner-only content. No new schema fields introduced. |
| T-10-04 | Information disclosure / NDA | Hero image content | mitigate | LICENSE-images.txt T-03-03 mitigation requires abstract/anonymised heroes (no employer screenshots). Task 1 acceptance criteria explicitly require abstract gradient/geometric image; Machinery Partner brand assets prohibited. |
| T-10-05 | Tampering / Build integrity | Bilingual parity (missing locale file would break next build for ALL case studies) | mitigate | `verifyParity()` in factory.ts throws at build if either .en.mdx or .pt.mdx sibling is missing. Task 2 acceptance criteria require both files to be authored together and `pnpm verify:projects` to pass before Task 3. |
| T-10-06 | DoS / Build throw | case-study-hero.tsx slug guard at line 33 (build-time throw if HERO_IMAGES entry missing) | mitigate | Task 3 acceptance criteria require atomic landing of hero image (Task 1) + MDX files (Task 2) + HERO_IMAGES entries (Task 3 Edits A/B/C). Build cannot succeed if any link is missing — explicit `pnpm build` verification in Task 3 catches mismatches. |
</threat_model>

<verification>
**Plan-level verification — run after all 3 tasks complete:**

1. **Build succeeds end-to-end:**
   - `pnpm build` exits 0 with "Compiled successfully" in output
   - Static generation includes `/projects/machinery-mobile-first` (visible in build output "Generating static pages" section)

2. **MDX content pipeline clean:**
   - `pnpm verify:projects` exits 0 (Zod validation + bilingual parity check covers all 5 case studies)

3. **HERO_IMAGES wiring atomic:**
   - `grep -c "'machinery-mobile-first': machineryMobileFirst" src/components/sections/case-study-hero.tsx` returns ≥ 1
   - `grep -c "'machinery-mobile-first': machineryMobileFirst" src/app/[locale]/projects/page.tsx` returns ≥ 1
   - `grep -c "'machinery-mobile-first': machineryMobileFirst" src/components/sections/featured-projects-teaser.tsx` returns ≥ 1

4. **i18n strings updated in both locales:**
   - `grep -c "Five case studies" messages/en.json` returns 2 (listingDescription + seo.descriptions.projects)
   - `grep -c "Four case studies" messages/en.json` returns 0
   - `grep -c "Cinco estudos de caso" messages/pt.json` returns 2
   - `grep -c "Quatro estudos de caso" messages/pt.json` returns 0

5. **Tests pass with no regression:**
   - `pnpm test:unit` exits 0 (all existing vitest tests pass)
   - `pnpm lint` exits 0 (biome check clean on all 8 modified files)

6. **Visual / runtime sanity (next dev manual smoke — non-blocking, optional):**
   - `pnpm dev` then visit http://localhost:3000/projects with NEXT_LOCALE=en cookie → 5 cards visible
   - Visit http://localhost:3000/projects/machinery-mobile-first → full case study renders with hero image, Problem/Solution/Impact/Stack sections, MDX toolkit components rendered
   - Set NEXT_LOCALE=pt cookie → same URL renders Portuguese content
   - View source on the case study page → `<script type="application/ld+json">` contains `"@type":"Article"` and `"headline":"Heavy Machinery mobile-first"` (or PT equivalent)
</verification>

<success_criteria>
- All 6 requirement IDs satisfied (CASE-15 through CASE-20)
- `pnpm build` exits 0
- `pnpm verify:projects` exits 0
- `pnpm test:unit` exits 0
- `pnpm lint` exits 0
- Both mobile-first MDX files exist with passing Zod frontmatter validation
- Hero image at content/projects/machinery-mobile-first/images/hero.jpg (1024x640 JPEG, < 50KB)
- HERO_IMAGES map extended in all three files: case-study-hero.tsx, projects/page.tsx, featured-projects-teaser.tsx
- JSON-LD Article schema already globally applied (changed in Phase 8; no new changes needed)
- "Five case studies" / "Cinco estudos de caso" i18n strings replace legacy "Four" / "Quatro" in both locale files (4 string updates total)
- At least one of <Callout>, <CodeFilename>, or <InlineBadge> appears in each MDX body (CASE-18)
- Both EN and PT case studies cover Problem / Solution / Impact / Stack with React Native + Expo narrative, offline cart-state contract, App Store review process, and mobile UX on low-connectivity (CASE-16 and CASE-17)

**Known deltas from ROADMAP wording (intentional, documented in 10-RESEARCH.md):**
- ROADMAP success criterion #1 mentions `/en/projects/machinery-mobile-first` and `/pt/projetos/machinery-mobile-first`. Actual codebase uses `localePrefix: 'never'` (i18n/routing.ts line 6) — the case study renders at `/projects/machinery-mobile-first` for BOTH locales (locale via NEXT_LOCALE cookie + Accept-Language header). This is a pre-existing routing decision, not a Phase 10 regression. The functional requirement (CASE-20: "user can visit and see the rendered case study in both locales") is fully met.
- The featured-projects-teaser on the home page shows only the first 3 featured projects (`.slice(0, 3)`). With `order: 5`, this case study will NOT appear on the home page teaser. It WILL appear on the full `/projects` listing page. This is intentional — the case study is a 5th entry, not a top-3 featured one.
</success_criteria>

<output>
After completion, create `.planning/phases/10-author-heavy-machinery-mobile-first/10-01-SUMMARY.md` covering:
- 6 requirements addressed (CASE-15 through CASE-20)
- Hero image generation approach (sharp / ImageMagick / manual gradient)
- Year confirmation TODO surfaced via MDX HTML comment
- i18n string changes (4 keys updated: "Four" → "Five" in both locales)
- Build + test + lint exit codes
- Any open follow-ups for Phase 11 (quality verification: a11y matrix, iPhone SE, Lighthouse, coverage)
</output>
